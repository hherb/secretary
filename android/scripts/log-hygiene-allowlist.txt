# Allowlist for check-log-hygiene.sh (#472).
#
# RULE A: any logcat sink (android.util.Log, println, System.out/err) outside
#   the sanctioned façade file. Not consulted here — logging at all outside
#   the façade is the violation, not a renderable value that could be reviewed
#   as safe. No entries exist or ever will for rule A.
# RULE B1: throwable-shaped constructs (.message / .localizedMessage /
#   .stackTraceToString() / .printStackTrace()) rendering a value that may
#   carry a carried diagnostic. Fail-closed and name-blind.
# RULE B2: no .toString() anywhere in the guarded scope. Fail-closed and
#   name-blind — the name-based form was the demonstrated bypass on iOS.
# RULE B3: no no-receiver `toString()` (implicit `this`) either. Same rule as
#   B2 for the form its leading dot could not see (#475).
# RULE C: no bare `$e` / `${e}` interpolation for a conventional catch
#   binding. Best effort — a denylist by necessity, see the script header.
#
# These are the reviewed exceptions.
#
# Format, one per line:
#   <repo-relative-path><TAB><rule: B1|B2|B3|C><TAB><exact trimmed source line><TAB><reason>
#
# The match is on the EXACT source line with leading/trailing whitespace
# stripped — NOT a substring. A substring entry exempts every future line in
# the same file that happens to contain it, which was demonstrably
# exploitable on the iOS sibling of this guard (#467). Re-indenting an
# exempted line keeps the entry valid; editing its content does not, which is
# the correct price for a line that bypasses the only mechanism keeping
# secrets out of logcat. A short needle is therefore inert rather than
# over-broad, which the `--self-test` pins by mutation (the X1/X2 pair).
#
# Blank lines and `#` comments are ignored.
#
# Adding an entry is a SECURITY DECISION. Justify it in the reason field.
#
# Three sections, ordered by how much review weight an entry carries:
#   SECURITY DECISIONS (rules B1, C) — a value that CAN carry a secret is
#     asserted safe to render here. (Rule A is an absolute prohibition with no
#     allowlist entries — see above.)
#   THE POLICY ITSELF (rule B3) — the two self-renders that ARE the sanctioned
#     rendering. Exempting the renderer from its own rule is not a hole; it is
#     where the rule bottoms out.
#   NON-THROWABLE RECEIVERS (rule B2) — a `.toString()` call whose receiver is
#     not throwable-shaped at all (a StringBuilder, a Uri, a Long).

# ---- SECURITY DECISIONS (rules B1, C) ----
# Each entry asserts that a value which CAN carry a secret is safe to render
# here. Reviewed like any other security change.

android/app/src/main/kotlin/org/secretary/app/CreateVaultWizardScreen.kt	B1	nameError?.let { Text(it.message ?: "Invalid name", modifier = Modifier.testTag("wizard-name-error")) }	Not a log and not a carried diagnostic: VaultNameError's arms are data objects whose message IS the user-facing copy ("Enter a name for the vault."). #454-compliant.
android/app/src/main/kotlin/org/secretary/app/CreateVaultWizardScreen.kt	B1	error?.let { Text(it.message ?: "Create failed", modifier = Modifier.testTag("wizard-error")) }	On-screen copy, not a log. VaultProvisioningError.CreateFailed IS a carried diagnostic, so this is a #454 violation — Android sibling of #473, filed not fixed (changing the copy is a UX decision). Payload is gated at construction as of this change.
android/app/src/main/kotlin/org/secretary/app/CreateVaultWizardScreen.kt	B1	error?.let { Text(it.message ?: "Error", modifier = Modifier.testTag("wizard-error")) }	Same as the line above — third wizard step, same VaultProvisioningError, same #473 sibling.
android/browse-ui/src/main/kotlin/org/secretary/browse/ui/RecordEditForm.kt	B1	val detail = it.message?.takeIf(String::isNotBlank) ?: it::class.simpleName	On-screen copy in a PACKAGE module, not a log. Renders a carried VaultBrowseError diagnostic — the clearest #473 sibling. Note the redactions do NOT affect `message`, so the user still sees which field is bad.

# ---- THE POLICY ITSELF (rule B3 only) ----
# Rule B3 denies a no-receiver `toString()` because inside a Throwable that is a
# self-render of the message the gate withholds. These two lines are where that
# self-render is the POINT: `diagnosticDetail` has decided the type is conformed
# and is now asking it to describe itself. Every other conformer's rendering
# flows through exactly these two lines, so if either is wrong the redactions
# elsewhere are moot — which is the argument for keeping them visible here
# rather than excluding the `diagnostics` package by path.

android/vault-access/src/main/kotlin/org/secretary/diagnostics/SecretFreeThrowable.kt	B3	get() = toString()	The interface's DEFAULT rendering — the single line that defines what "conformed, not redacting" means. Reachable only after `error is SecretFreeThrowable`, i.e. after a declaration-site security decision.
android/vault-access/src/main/kotlin/org/secretary/browse/VaultBrowseError.kt	B3	else -> toString()	The non-redacted arms of VaultBrowseError's own override. The three arms that CAN carry a decrypted field name (CorruptVault, SaveCryptoFailure, InvalidArgument) are matched ABOVE this line and never reach it; see the payload-origin audit on `diagnosticDescription`.

# ---- NON-THROWABLE RECEIVERS (rule B2 only) ----
# Each entry asserts ONLY that the receiver is not a Throwable — a two-second
# check against the surrounding code, not a security argument. Kept separate so
# the SECURITY DECISIONS section above stays short enough that its entries keep
# their weight. The durable fix is a type-aware lint; see the spec's Non-goals.

android/app/src/main/kotlin/org/secretary/app/AppRoot.kt	B2	selectionVm.recordSelection(VaultLocation(label, uri.toString()))	Receiver is an android.net.Uri from the SAF picker.
android/app/src/main/kotlin/org/secretary/app/AppRoot.kt	B2	pickedTreeUri = uri.toString()	Receiver is an android.net.Uri from the SAF picker.
android/browse-ui/src/main/kotlin/org/secretary/browse/ui/SettingsScreen.kt	B2	value = value.toString(),	Receiver is a numeric settings value bound to a TextField.
android/vault-access/src/main/kotlin/org/secretary/mirror/VaultMirror.kt	B2	base.relativize(file.toPath()).toString().replace(File.separatorChar, '/') to file.readBytes()	Receiver is a java.nio.file.Path being normalised to a vault-relative key.
