{
  "$schema": "https://schema.tauri.app/config/2.0.0/capability",
  "identifier": "default",
  "description": "Default capabilities for the secretary-desktop main window. Grants the bare minimum the D.1.2 walking skeleton needs: Tauri core (event listening, IPC invoke), the open-folder dialog used by PathPicker, and clipboard WRITE for the copy-secret affordance (D.1.2 Task 6). Clipboard read is intentionally NOT granted — reading the clipboard is a sensitive capability for a secrets manager.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "clipboard-manager:allow-write-text"
  ]
}
