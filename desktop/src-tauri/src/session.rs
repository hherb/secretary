//! `VaultSession` — the live cryptographic state holder.
//!
//! Module skeleton landing in the foundation commit; the real implementation
//! (unlock / lock / notify_activity / set_settings + the `UnlockedSession`
//! Drop chain) ships in the follow-up commit so the foundation diff stays a
//! pure refactor.
