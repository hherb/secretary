# Allowlist for check-public-log-hygiene.sh (#467).
#
# The guard is FAIL-CLOSED: every `privacy: .public` interpolation must render
# through `diagnosticDetail(`. These are the reviewed exceptions.
#
# Format, one per line:   <repo-relative-path><TAB><required substring><TAB><reason>
# A `.public` line in that file is accepted ONLY if it contains that substring.
# Anything else in that same file still fails. Blank lines and `#` comments ignored.
#
# Adding an entry is a SECURITY DECISION — it exempts a line from the only
# mechanism that keeps secrets out of the unified log store. Justify it here.

ios/SecretaryVaultAccess/Sources/SecretaryVaultAccessUI/DiagnosticLog.swift	foldedErrorDiagnostic(	The fold-site formatter. It calls diagnosticDetail internally, proven byte-exact by DiagnosticLogTests.testDiagnosticIsSiteAndDetailOnly and testFormatterAppliesDefaultDeny — so it IS gated, one level of indirection down.
ios/SecretaryKit/Sources/SecretaryKit/VaultAccess/BookmarkVaultLocationStore.swift	location.displayName	Not an error: the vault FOLDER name from the system picker. Anyone able to read the unified log can read the filesystem, so it discloses nothing new; VaultLocation's own doc records that no key or credential flows through the type.
