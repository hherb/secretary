"""§4.1/§6.1 binary envelope parsers, and the golden-vault verification path.

These parse a file in order to INSPECT it. The round-trip decode/encode pairs
behind `--diff-replay` live in `conformance_lib.codec` instead.
"""
