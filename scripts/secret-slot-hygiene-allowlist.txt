# Allowlist for scripts/check-secret-slot-hygiene.sh (#521).
#
# FORMAT — four TAB-separated fields:
#   <repo-relative path>\t<rule id: S1|S2>\t<exact trimmed source line>\t<justification>
#
# Keyed on the EXACT TRIMMED SOURCE LINE, never a substring. A substring entry
# would exempt every future line in the same file containing the same needle,
# which was demonstrably exploitable against the iOS guard (#467, third review
# round). Re-indenting an exempted line keeps its entry valid; editing its
# content does not, so a bypass line cannot be quietly repurposed.
#
# ADDING AN ENTRY IS A SECURITY DECISION. These identifiers do not merely leak
# a secret — they make a zeroize-on-drop wrapper's wipe VACUOUS, so reviewed
# code keeps looking protected while it is not. Prefer restructuring the call
# site over adding a row here.
#
# KEEPING THIS LIST SHORT IS WHAT KEEPS ITS ENTRIES MEANINGFUL.
#
# An earlier version of this header said the file "SHIPS EMPTY … the census
# found exactly one hit tree-wide". Both halves were wrong, and the reason is
# worth recording rather than quietly deleting: the census was run over the
# six roots the guard happened to scan, not over the tree. It omitted two
# workspace MEMBERS — `browser/secretary-browser-host` (secret-bearing: it
# handles the device secret and the master password through `SecretBytes`)
# and `desktop/secretary-desktop-presence`. Both are scanned now, and the
# browser host's two `scrub_string` sites are the entries below. "Empty" was
# a property of the root list, not of the tree.
#
# The tree-wide census across the CURRENT roots is three: the two rows below
# plus `core/src/crypto/secret.rs`'s own doc comment, which is prose and is
# handled by `is_comment_line` rather than by a row here.

browser/secretary-browser-host/src/enroll.rs	S1	let mut buf = std::mem::take(s).into_bytes();	scrub_string: the move-out IS the wipe. `String` has no safe in-place byte-mut (`as_bytes_mut` is unsafe, and the workspace forbids unsafe), so taking the owned buffer, zeroing it and dropping it is the only safe scrub. `s` is left empty, not holding a live secret, so no wrapper's Drop is made vacuous. Reviewed 2026-08-23.
browser/secretary-browser-host/src/bin/enroll.rs	S1	let mut buf = std::mem::take(s).into_bytes();	Duplicate of the library `scrub_string` above, in the enroll binary. Same justification; the duplication itself is tracked separately (#549).
