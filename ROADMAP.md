# Secretary — Roadmap

This roadmap shows where Secretary is today and what comes next. It is meant for potential contributors deciding whether and where to help, and for users trying to gauge when the project will be usable.

The project is structured as four sequential sub-projects. Each sub-project is a coherent unit of work that can be reviewed end-to-end before the next begins. **Sub-project A** is the Rust cryptographic core and on-disk format. **Sub-project B** is the FFI binding layer (PyO3 + uniffi). **Sub-project C** is the headless sync-orchestration layer — file watching, cloud-folder integration, conflict-detection scheduling — exposed across the FFI so every UI gets the same orchestration semantics for free. **Sub-project D** is the platform UIs.

This ordering is deliberate. The cryptographic core is the only piece where mistakes are hard to walk back — once a vault format is in the wild and people have stored multi-decade secrets in it, you cannot fix a flaw without a forced migration. So the core ships first, with rigour. Sync orchestration sits between FFI and the UIs as its own phase rather than being folded into per-platform UI work, so the orchestration logic is built and tested once (with a headless `secretary sync` CLI as a reference consumer) instead of being re-invented and re-debugged across desktop, iOS, and Android.

For the full design specifications see [docs/](docs/). For the next-session entry point with concrete TODOs see [secretary_next_session.md](secretary_next_session.md).

---

## Where we are: 2026-06-14

- **Sub-project A**: feature-complete for v1. Internal hardening track (fuzzing, threat-model refresh, side-channel audit, memory-hygiene audit) ✅ closed. Only the external paid review track remains.
- **Sub-project B**: complete through B.6 v2 + #187 + B.1 device-wrap slot + B.2 FFI projection + B.3 iOS Secure Enclave unlock. Bridge crate + PyO3 + uniffi (Swift, Kotlin) expose unlock / open / read / save / share / trash / restore + sync surface + device-slot ops. iOS device-unlock orchestration host-tested in `ios/SecretaryDeviceUnlock/`; SE conformer compile-verified on simulator; on-device biometric proof is [#202](https://github.com/hherb/secretary/issues/202). Cross-language conformance KAT (26 vectors + enrol round-trip, lifecycle-complete) replays Rust ↔ Swift ↔ Kotlin parity. **Block-CRUD tier ✅ shipped 2026-06-19** — `create_block` / `rename_block` / `move_record` added to the uniffi write surface. `rename_block` changes only the block name (records + every `unknown` preserved); `move_record` is a **faithful move** — copy a live record into a target block under a caller-minted fresh `record_uuid` (preserving `created_at_ms`, per-field `last_mod`/`device_uuid`, values, and all `unknown`), then tombstone the source, copy-before-delete so a missing target leaves the source live. Caller-mints all UUIDs (void returns); same-block + uuid-length checks live at the uniffi wrapper (existing `VaultError::InvalidArgument`), **no new `FfiVaultError` variant**. Host-tested: cargo (bridge + uniffi) + Swift/Kotlin conformance & smoke + Android `:kit` + iOS XCTest; no `core` / on-disk-format / crypto-spec change.
- **Sub-project C**: C.1 phase 1 (sync detection), C.1.1a (conflict-copy ingestion), C.1.1b (merge layer), C.2 (headless `secretary-sync` CLI) ✅ complete. The CLI now runs end-to-end against a real vault folder (`once` + `run` subcommands; `notify`-driven daemon loop with trailing-edge debounce, ready-window gate, host-local lockfile, signal-driven shutdown; two-instance convergence + cross-platform `notify` quirk pinned in CI). C.3 (mobile adapters): the **iOS** stack shipped end-to-end (orchestration core + folder-change detection + sync UI — badge, sync-at-unlock, conflict-resolution sheet); the **Android** orchestration core ✅ shipped (slice 1 — `android/vault-access`, the repo's first Gradle project) and its real `UniffiVaultSyncPort` adapter ✅ shipped (slice 2a — the `:kit` module, host/build-verified), its emulator round-trip ✅ shipped (slice 2b — golden vault through the native `.so` via `UniffiVaultSyncPort` + `SyncCoordinator` on `Medium_Phone_API_36.1`), its folder-change detection ✅ shipped (slice 3 — a host-tested `FolderChangeDetector` debounce core + a real `FileObserver` watcher, foreground-only, with an emulator smoke), its sync-UI model ✅ shipped (slice 4), its **Compose sync render ✅ shipped 2026-06-16** (slice 5 — FFI-free `:sync-ui` module with `SyncBadge` / `SyncPasswordSheet` / `ConflictResolutionSheet` / `SyncScreen` over the slice-4 `VaultSyncModel`; first Compose UI-test harness on Android, 15 instrumented tests), and its **first runnable `:app` walking skeleton ✅ shipped 2026-06-17** (slice 6 — a Compose `:app` hosting the slice-5 `SyncScreen` over the **real** `makeVaultSync` lifecycle: unlock → silent `syncAtUnlock` → badge, monitor bound to the Sync screen's composition, `FLAG_SECURE`, a bundled writable `golden_vault_001` demo vault, proven by an on-device `MakeVaultSyncSmokeTest`), and its **vault open/browse ✅ shipped 2026-06-17** (slice 7 — real `open_vault_with_password` → metadata-only Compose `BrowseScreen` listing blocks and record titles/types/tags/field-names; no secret value crosses the adapter; lock-on-background wipes the session; new `:browse-ui` module; 4/4 on-device), and its **reveal-on-tap ✅ shipped 2026-06-17** (slice 8 — the first Android slice where a secret value crosses the adapter: per-field `expose_text`/`expose_bytes` on tap via a retained `BlockReadOutput`, 30s auto-hide, tap-to-hide, drop-on-background; the only `expose_*` call sites live in one reveal lambda and `wipe()` zeroizes blocks → manifest → identity; host-tested + an instrumented Compose UI test + an on-device smoke that asserts the real `.so` reveals the golden vault's `hunter2`), and its **soft-delete lifecycle ✅ shipped 2026-06-18** (slice 9 — the first Android slice that **writes** to a vault: a show-deleted toggle + per-row tombstone/resurrect over the existing uniffi write surface, a file-backed device-UUID under `noBackupFilesDir`, writes serialized under the same session lock/`wiped` guard as reads, proven by an on-device round-trip smoke), and its **record add + edit ✅ shipped 2026-06-18** (slice 10 — `RecordEditForm` + `RecordEditModel` mirroring iOS `RecordEditViewModel`: free-text record type, editable tags, per-field Text/Bytes kind picker for text + bytes-as-hex fields, add/remove fields, per-row edit; drives `appendRecord`/`editRecord` via the slice-9 write infra; on-device add/edit round-trip smoke; no `core` / `ffi` / on-disk-format change), and its **sync-on-browse ✅ shipped 2026-06-18** — sync badge + sync-at-unlock re-integrated onto the browse screen via `BrowseWithSyncScreen` (reused `SyncScreen` stacked above `BrowseScreen`; background sync-at-unlock via `launchSyncAtUnlock`; monitor lifecycle bound to Browse composition; both platforms run a separate sync-pass Argon2id, mitigated by background execution; Android-specific delta: cannot reuse the open session even for the vault UUID, provisions it via `goldenVaultUuid`; mirrors iOS's unified `VaultBrowseScreen`; Android-only, no `core` / `ffi` / on-disk-format change), and its **recovery-phrase open ✅ shipped 2026-06-19** (a second unlock credential — the 24-word BIP-39 recovery phrase — alongside the password open, both reaching the unified `BrowseWithSyncScreen` via a sealed `UnlockCredential` + pure `openWithCredential` dispatch; `:kit` `openWithRecovery` over the generated `openVaultWithRecovery`; new `WrongRecoveryOrCorrupt` (conflated §13) + `InvalidRecoveryPhrase` typed errors; recovery sessions get a status-only sync badge since Android sync is password-keyed, and sync manually via the badge re-prompt, mirroring iOS; host-tested + an on-device recovery-open smoke; Android-only, no `core` / `ffi` / on-disk-format / UDL change), and its **device-secret open slice 1 ✅ shipped 2026-06-19** (pure `DeviceUnlockCoordinator` + ports + `:kit` FFI adapter, proven against the real `.so` with a fake in-memory enclave; real biometric Keystore enclave + UnlockScreen affordances = slice 2; Android-only, no `core` / `ffi` / on-disk-format / UDL change), and its **real biometric device open slice 2 ✅ shipped 2026-06-19** (`KeystoreDeviceSecretEnclave` AES-256-GCM Keystore key gated by `BiometricPrompt`/`CryptoObject`; `FileDeviceEnrollmentMetadataStore`; `BiometricPromptGate` + `mapBiometricError`; `FragmentActivity` migration; `UnlockScreen` biometric affordances + `AppRoot` enroll/unlock wiring; automated coverage green; **real-biometric on-device proof verified on an NX809J / Android 16** — enrol → kill → biometric-only open → `BrowseWithSyncScreen`, plus a clean cancel — the Android analogue of iOS #202; Android-only, no `core` / `ffi` / on-disk-format / UDL change), and its **device-management Settings UI ✅ shipped 2026-06-19** (a "Device settings" screen reachable from the Browse screen showing this device's biometric-enrollment status; lets the user enroll — with a vault-password re-prompt — or disenroll this device; Android-only, no `core` / `ffi` / on-disk-format / UDL change), and its **biometric write re-auth ✅ shipped 2026-06-21** (every mutating vault write gated by a 30 s grace-window biometric presence proof when device-secret is enrolled; pure Kotlin `WriteReauthGate`/`GraceWindowReauthGate`/`NoopReauthGate` in `:vault-access` + real `CoordinatorBiometricAuthorizer` in `:vault-access`; cancel silent, other failure surfaces `VaultBrowseError.ReauthFailed`; gate seeded at open, reset on lock; Android-only, no `core` / `ffi` / on-disk-format change). C.4 (cross-device convergence conformance) ✅ 2026-06-15 — two device identities reconciling through a shared folder converge to identical logical state, order-independent, over the real pipeline (`core/tests/convergence.rs`).
- **Sub-project D**: D.1.1 Tauri walking skeleton ✅ shipped (2026-05-29) — unlock-an-existing-vault flow + block-list scaffold + vault-stored auto-lock timeout + auto-lock timer + lock button, as one Tauri 2 codebase (Rust backend + Svelte/TypeScript frontend). Landed across 12 task PRs (#130 spec/ADR → #157 app orchestration); the L4 end-to-end test is manual-only (not in CI) and deferred ([#161](https://github.com/hherb/secretary/issues/161)). **D.1.2 (read-only browse) ✅ shipped (2026-05-29)** — clickable block detail → record list → field viewer with per-field reveal/mask, auto-hide, and copy-with-auto-clear, over a stateless re-decrypt IPC path. **D.1.3 (vault create wizard) ✅ shipped (2026-05-29)** — 3-step wizard (folder pick + probe, credentials, mnemonic backup); App routes to wizard when `appRoute=create`; Unlock turns the "Not a vault" hint into a "Create a vault here" button and shows a "Vault created" banner with the path pre-filled after create. **D.1.4 (vault edit — add/edit records) ✅ shipped (2026-05-30)** — native-BlockPlaintext bridge primitives (`create_block`/`append_record`/`edit_record`); untouched records + all `unknown` maps survive byte-faithfully; four IPC commands (`create_block`/`save_record`/`save_record_edit`/`reveal_record`); record editor UI (text + base64-bytes fields, tags, type); closes #141 (`RecordInput` gains `record_type` + `tags`). **D.1.5 (delete/trash) ✅ shipped (2026-05-31)** — record tombstone/resurrect bridge primitives over the lossless write path (siblings + all three `unknown` levels preserved across delete *and* resurrect); `list_trashed_blocks` (decrypts each trashed block for its name); five IPC commands (`tombstone_record`/`resurrect_record`/`trash_block`/`restore_block`/`list_trashed_blocks`) wrapping the new + pre-existing (B-phase) bridge orchestrators; tombstoned-record visibility **Rust-gated** via an `include_deleted` read parameter (the "Show deleted" toggle re-reads — the client never filters withheld data); confirm-on-destructive UI + a Trash view. The original D.1.5 "share" was split out (it needs a desktop contacts subsystem). **D.1.6 (share a block + contacts subsystem) ✅ shipped (2026-05-31)** — three bridge-thick primitives (`enumerate_contact_cards`/`import_contact_card`/`share_block_to`) own all `contacts/` I/O: TOFU import parses then `verify_self`-checks both Ed25519 ∧ ML-DSA-65 halves (mirroring `restore_block`) and dedup-rejects a duplicate `contact_uuid`; `share_block_to` assembles the recipient set from disk and delegates to the existing `share_block` wrapper (zeroize discipline unchanged); three IPC commands + five typed `AppError`s + a redacting `ContactSummaryDto` that keeps card bytes/keys server-side; a `ShareDialog` (picker + inline file-import) opened from a block's 🔗 action. Two new `FfiVaultError` variants (`ContactAlreadyExists`/`ContactNotFound`) were threaded through the uniffi + pyo3 bindings; the contacts *functions* remain bridge-only ([#167](https://github.com/hherb/secretary/issues/167)); no conformance-KAT change. **D.1.7 (contacts management) ✅ shipped (2026-06-03)** — `owner_card_export` (serialize the owner's PUBLIC card → canonical filename + bytes) + `delete_contact_card` (owner-guarded; `CannotDeleteOwnerContact`) bridge primitives, plus `enumerate_contact_cards` widened with a per-contact `shared_block_count` (in-memory recipient scan); a Contacts pane (👤) that exports your card to a chosen folder via the already-granted folder picker (native write, no new capability) and lists imported contacts with their recipient counts, with a warn-but-allow delete whose confirm makes explicit that **delete ≠ revoke** (the former recipient keeps access to already-shared blocks). One new `FfiVaultError` variant (`CannotDeleteOwnerContact`) threaded through uniffi + pyo3 + the KAT helper + desktop; contacts *functions* still bridge-only ([#167](https://github.com/hherb/secretary/issues/167)); no conformance-KAT change. **D.1.8 (per-block recipients) ✅ shipped (2026-06-04)** — a read-only `block_recipients` bridge primitive projects a block's `recipients[]` into classified summaries (Owner / Contact{name} / Unknown), reading + both-halves-`verify_self`-checking each `contacts/*.card` so a tampered or missing card can never surface a trusted name; a redacting `RecipientDto` + one read-only IPC command + a collapsible "Shared with" banner in the records view show who a block is shared with (owner as "You"; a deleted contact's still-shared block as "Unknown contact (uuid…)", making the D.1.7 "delete ≠ revoke" residual-keyholder state visible). No `core` change, no new `FfiVaultError` variant, no UDL/Swift/Kotlin/pyo3 change (contacts functions stay bridge-only, [#167](https://github.com/hherb/secretary/issues/167)). Revoke/unshare deferred — it needs a frozen-`core` revoke primitive that does not exist ([#177](https://github.com/hherb/secretary/issues/177)). **D.1.9 (per-contact reverse map) ✅ shipped (2026-06-04)** — a read-only `contact_blocks` bridge primitive inverts the same `manifest.blocks[].recipients` scan that powers `shared_block_count` (so `contact_blocks(uuid).len() == shared_block_count` by construction); a `list_contact_blocks` IPC command; an inline, lazily-fetched (fetch-on-first-expand, cached) reverse map on each ContactRow in the Contacts pane, so a user can expand a contact to see every block they receive and click one to open it. No `core` change, no new `FfiVaultError` variant, no UDL/Swift/Kotlin/pyo3 change (contacts functions stay bridge-only, [#167](https://github.com/hherb/secretary/issues/167)). Revoke/unshare still deferred ([#177](https://github.com/hherb/secretary/issues/177)). **D.1.10 (revoke primitive — frozen-`core`, closes [#177](https://github.com/hherb/secretary/issues/177)) ✅ shipped (2026-06-05)** — `revoke_block_recipient` is the inverse of `share_block`: it rotates the block content key, re-wraps for the *remaining* recipients only, drops the revoked uuid from the manifest `BlockEntry.recipients`, re-signs Ed25519 ∧ ML-DSA-65 and atomic-writes block-then-manifest. The re-key engine was extracted from `share_block` into a shared `rewrite_block_with_recipients` helper (behavior-preserving; the existing share suite is the guard). A fail-fast `CannotRevokeOwner` guard rejects revoking the owner — the owner is *always* a recipient (must decrypt to re-key), so revoking it would brick the block; revoke-to-empty is therefore impossible. A bridge `revoke_block` / `revoke_block_from` mirrors the share surface (zeroize discipline + exhaustive typed-error mapping unchanged). Two new typed errors (`RecipientNotPresent`, `CannotRevokeOwner`) threaded through `VaultError` → `FfiVaultError` → UDL → pyo3 → uniffi → desktop + the Swift/Kotlin conformance harnesses. On-disk format unchanged (no `format_version` bump — a revoke emits the same bytes as a share to a smaller set); `vault-format` §6.5.1 + `crypto-design` §7.3 document revocation + the forward-secrecy boundary, and a stdlib-only revoke KAT in `conformance.py` proves the re-key cross-language. **D.1.11 (desktop revoke UI) ✅ shipped (2026-06-05)** — an always-visible "Revoke" ✕ on both share surfaces (the per-block "Shared with" banner's non-owner rows and the per-contact reverse map's block rows), gated behind a confirm dialog that states the forward-secrecy boundary explicitly; a `revoke_block_from` IPC command over the D.1.10 bridge primitive, strict typed-error surfacing on the mutation path (no read-path leniency), the owner never offered a control, and a surface-local refresh of the banner / contact shared-block count on success. No `core`/bridge change (the bridge `revoke_block_from` + the two typed errors shipped in D.1.10). **D.1.12 (desktop polish batch) ✅ shipped (2026-06-05)** — a debt-clearing pre-external-ship slice over four carry-forward issues: a vendored inline-SVG icon system ([#154](https://github.com/hherb/secretary/issues/154)) — eight hand-authored Lucide components (`currentColor` so light/dark themes work for free, no runtime dependency) replace every color-emoji icon, which rendered as color glyphs on macOS/Windows but tofu on bare-Linux installs; `aria-controls` paired with the existing `aria-expanded` on both disclosure toggles ([#180](https://github.com/hherb/secretary/issues/180)), each pointing at a uuid-derived region id; an `Esc`-pops-a-browse-level affordance ([#164](https://github.com/hherb/secretary/issues/164)) factored as a pure `shouldPopOnEscape(level, dialogOpen, inFormControl)` guard (truth-tabled over all eight browse levels) wired to a window keydown via `$effect`, popping only at the read-only `records`/`fields` levels and no-op at the root, form levels, with a native `<dialog>` open, or while a form control has focus; and the duplicated `lock_session` session-lock helper hoisted into `commands::shared` ([#170](https://github.com/hherb/secretary/issues/170)) and adopted across all eight command modules (15 sites). No `core` / `ffi` / bridge / `FfiVaultError` / UDL change — a pure desktop slice, so no cross-language conformance run. **D.1.13 (sync bridge primitive) ✅ shipped (2026-06-06)** — a feature-gated `secretary-cli` `sync_pass_pause_on_conflict` orchestration that auto-applies every safe sync arm (nothing-to-do / fast-forward / silent-merge / concurrent-with-zero-vetoes) but returns `ConflictsPending` writing nothing the moment a tombstone veto needs a human decision; a bridge `sync_status` read primitive (per-vault `SyncState` projection, no secrets) + a `sync_vault` mutation (opens a core identity from a re-prompted password, holds the per-vault lockfile, persists state on advancing arms only); five new `FfiVaultError` sync variants threaded through every binding; a cross-language `sync_pass` classification KAT (Rust guard + clean-room `conformance.py` replay). No `core`/on-disk-format/crypto change; sync functions bridge-only at ship; uniffi/pyo3 projection shipped separately ([#187](https://github.com/hherb/secretary/issues/187) ✅ 2026-06-09, pairs with [#167](https://github.com/hherb/secretary/issues/167)). **D.1.14 (desktop sync UI) ✅ shipped (2026-06-07)** — TopBar "sync pill" showing "Synced …"/"Never synced" that doubles as the "Sync now" trigger + a centered password re-prompt modal; `sync_status`/`sync_now` Tauri commands; desktop DTOs; the five D.1.13 sync `AppError` codes threaded into the TS layer; no core/ffi/FfiVaultError/UDL/bridge change — pure desktop slice over D.1.13's primitive. **D.1.15 (interactive conflict resolution) ✅ shipped (2026-06-08)** — makes the D.1.13/D.1.14 `ConflictsPending` outcome actionable instead of a dead-end "N conflicts — coming soon": when "Sync now" hits a record-tombstone veto a centered resolution modal lists each disputed record by **metadata only** (record type · tags · field names · timestamps · which device deleted it — no secret values) with a per-record **Keep mine / Accept delete** choice (default Keep mine, so no silent data loss) plus a read-only "auto-merged" notice for field-level LWW collisions, then commits the decisions. A **stateless recompute-on-commit** two-call flow: call-1 (`sync_now`/`sync_pass_inspect`) returns the veto + collision metadata + a `manifest_hash` freshness token committing nothing; the user decides; call-2 (`sync_commit_decisions`/`sync_pass_commit_decisions`) recomputes the deterministic draft, asserts the token still matches (`EvidenceStale` on a mid-modal disk change — no clobber) and commits. Spans six layers — `core` (a metadata-only `RecordCollisionSummary` on `DraftMerge`, no secret values) + `secretary-cli` (`sync_pass_inspect` / `sync_pass_commit_decisions`) + bridge (conflict-detail DTOs `VetoDto`/`CollisionDto`/`VetoDecisionDto`, `sync_commit_decisions`, a `SyncDecisionsIncomplete` error un-collapsed from the generic `SyncFailed`) + uniffi/pyo3 (the new `FfiVaultError` variant threaded through all bindings + the Swift/Kotlin conformance harnesses, both green) + desktop (a `sync_commit_decisions` Tauri command, DTOs, a typed `SyncDecisionsIncomplete` AppError, the `ConflictResolutionDialog` wired into the SyncPill flow reusing the sync-modal password — no second prompt, cleared on every terminal path). The commit happy-path + stale-token rejection are covered at the cli layer; the manual GUI smoke remains the one outstanding pre-merge gate ([#161](https://github.com/hherb/secretary/issues/161), like D.1.14). **D.3 slice 1 (iOS XCFramework + linked-call proof) ✅ shipped (2026-06-10)** — bootstraps the native-iOS path ([ADR 0008](docs/adr/0008-native-mobile-via-uniffi.md)) with a reproducible build pipeline and an automated on-simulator linked-call proof. A `staticlib` crate-type added to `secretary-ffi-uniffi` (additive — the existing cdylib/rlib consumers + the full conformance gauntlet are unchanged); `ios/scripts/build-xcframework.sh` cross-compiles that staticlib for the three iOS triples (aarch64 device + aarch64-sim + x86_64-sim), lipo's the simulator slices, generates the Swift bindings, and assembles `ios/Secretary.xcframework` (device + simulator) while staging golden_vault_001 as a test resource; `ios/SecretaryKit/` is a Swift Package (a `binaryTarget` for the XCFramework, a `SecretaryKit` library wrapping the generated `secretary.swift`, and an XCTest target that opens golden_vault_001 on a simulator and asserts the pinned vault UUID + a wrong-password typed error); `ios/scripts/run-ios-tests.sh` is the acceptance entry point (builds then runs the XCTest on a simulator — default `iPhone 16`, override via `IOS_SIM`). No app UI and no Keychain/Secure-Enclave key storage yet — that is the next slice.

```
[================================================================] Sub-project A — Rust core (feature-complete; A.7 internal track closed; external review pending)
[================================================================] Sub-project B — FFI bindings (B.1 → B.6 v2 ✅)
[==================================                              ] Sub-project C — Sync orchestration (C.1 + C.1.1a/b ✅; C.2 ✅; C.3 iOS ✅, Android core + adapter + emulator round-trip + folder-watch + sync-UI model + Compose render + `:app` walking skeleton + vault open/browse + reveal-on-tap + soft-delete + record add/edit + sync-on-browse + recovery-open + device-open slice 1 ✅ + device-open slice 2 biometric ✅ + device-settings UI ✅ + biometric write re-auth ✅; C.4 ✅)
[======                                                          ] Sub-project D — Platform UIs (D.1.1 walking skeleton + D.1.2 browse + D.1.3 create wizard + D.1.4 vault edit + D.1.5 delete/trash + D.1.6 share/contacts + D.1.7 contacts management + D.1.8 per-block recipients + D.1.9 per-contact reverse map + D.1.10 revoke primitive + D.1.11 revoke UI + D.1.12 desktop polish batch + D.1.13 sync primitive + D.1.14 sync UI + D.1.15 interactive conflict resolution + D.3 s1 iOS link-proof + B.3 iOS SE device unlock + iOS app walking-skeleton + #202 on-device biometric proof + iOS password/recovery unlock + read-only browse + iOS vault selection + iOS record CRUD + iOS vault create/import + iOS include_deleted Rust gate + iOS KDF off the main actor + iOS biometric write re-auth ✅ + desktop password re-auth before writes ✅ + desktop write-gate coverage guard ✅ + Android biometric write re-auth ✅; OS-biometric on desktop + configurable grace-window settings + presence proof for password-only sessions pending)
```

Test totals as of B.6 v2 / C.1.1b: 800 tests pass + 10 ignored under `cargo test --release --workspace`; 68 pytest; 38 Swift / 39 Kotlin smoke asserts; 22/22 cross-language conformance vectors PASS on both Swift and Kotlin. Clippy clean with `-D warnings --tests`. `#![forbid(unsafe_code)]` workspace-wide except a localized `deny` carve-out in the two binding-flavor crates.

---

## Sub-project A — Rust cryptographic core

The core lives in [core/](core/) (crate name `secretary-core`). It is the single source of truth for everything security-relevant: cryptographic primitives, identity, vault format read/write, recipient handling, conflict resolution.

### Phase A.1 — Cryptographic primitives ✅

NIST KAT-pinned v1 cipher suite: Argon2id (RFC 9106), XChaCha20-Poly1305, HKDF-SHA-256 (RFC 5869), BLAKE3, Ed25519 (RFC 8032), X25519 (RFC 7748), ML-KEM-768 (FIPS 203), ML-DSA-65 (FIPS 204). Hybrid constructions (X25519 ⊕ ML-KEM-768; Ed25519 ∧ ML-DSA-65, AND not OR) KAT-pinned. Vectors in [core/tests/data/](core/tests/data/).

### Phase A.2 — Identity ✅

`secretary_core::identity`: identity seed, per-role key derivation (auth, KEM, recovery), Contact Cards, recipient fingerprints, position-specific signature roles (`SigRole::Block` ≠ `SigRole::Manifest`) so signatures cannot be cross-purpose replayed.

### Phase A.3 — Unlock module ✅

`secretary_core::unlock`: BIP-39 24-word mnemonic, identity bundle (master KEK + recovery KEK dual wrap), `vault.toml` metadata, orchestrators (`create_vault`, `open_with_password`, `open_with_recovery`). Argon2id v1 floor enforced at vault creation as a typed error (`UnlockError::WeakKdfParams`); the open path is not floor-gated (a downgraded `vault.toml` is defeated by the differing Master KEK and the signed-manifest `[kdf]` cross-check — see threat-model.md §3.2).

### Phase A.4 — Vault block format ✅

`secretary_core::vault::{record, block}`: typed records with canonical CBOR (RFC 8949 §4.2.1) and a forward-compat `UnknownValue` for bit-identical round-trips; binary block file (header + recipient table + AEAD body + hybrid signature suffix); verify-before-decap structurally enforced. §15 conformance via [block_kat.json](core/tests/data/block_kat.json) replayed by the clean-room Python verifier.

### Phase A.5 — Vault manifest + orchestrators ✅

`secretary_core::vault::{manifest, io, orchestrators, canonical}`: manifest format per `docs/vault-format.md` §4; atomic writes via `tempfile::NamedTempFile::persist` (exact-pinned `=3.27.0` as a security-critical dep); typed vector-clock overflow; high-level orchestrators (`create_vault`, `open_vault`, `save_block`, `share_block`) with §9 atomic write ordering. §15 closure via [golden_vault_001/](core/tests/data/golden_vault_001/) end-to-end conformance.

### Phase A.6 — CRDT merge primitives ✅

`secretary_core::vault::conflict`: pure-function vector-clock primitives (`clock_relation`, `merge_vector_clocks`, `merge_record`, `merge_block`); record-level `tombstoned_at_ms` death-clock closes the three-way-merge associativity gap; §11.3 identity-metadata override on tombstone-wins; defensive bidirectional clamp of `tombstoned_at_ms` against malformed peers. Four proptests (commutativity, associativity, idempotence, well-formedness Property L) hold on the full record domain. §15 KAT: 11 vectors in [conflict_kat.json](core/tests/data/conflict_kat.json), replayed cross-language by a clean-room Python `py_merge_record` / `py_merge_unknown_map`.

### Phase A.7 — Hardening + audit prep 🚧 (internal ✅; external pending)

**Internal track (all closed 2026-05-02):**

- **Fuzz harness** ✅ — six `cargo-fuzz` targets (`vault_toml`, `record`, `contact_card`, `bundle_file`, `manifest_file`, `block_file`) under [core/fuzz/](core/fuzz/); promoted regressions replay through `cargo test`. NiceGUI dashboard with live telemetry (`core/fuzz/monitor.py`). Cross-language differential-replay protocol at [`docs/manual/contributors/differential-replay-protocol.md`](docs/manual/contributors/differential-replay-protocol.md).
- **Threat-model refresh** ✅ — `docs/threat-model.md` realigned with implementation; four divergences fixed (notably §3.5 mixed-suite-IDs); §5 verification trace grown to ~30 cited test names, all verified to exist.
- **Side-channel audit** ✅ — memo at [`docs/manual/contributors/side-channel-audit-internal.md`](docs/manual/contributors/side-channel-audit-internal.md). All CT-sensitive comparisons delegate to upstream RustCrypto; principal output is the upstream-assumption list for the paid reviewer.
- **Memory-hygiene audit** ✅ — memo at [`docs/manual/contributors/memory-hygiene-audit-internal.md`](docs/manual/contributors/memory-hygiene-audit-internal.md). Twelve stack-residue gaps closed (`var.zeroize()` after `Sensitive::new(var)` pattern); `RecordFieldValue::{Text, Bytes}` now wrap `SecretString` / `SecretBytes` (PR #16); `MlDsa65Secret` / `MlKem768Secret` newtypes derive `Zeroize, ZeroizeOnDrop`.
- **User-facing primer** (PR #10) ✅ — thirteen-chapter [cryptography primer](docs/manual/primer/cryptography/index.md) + [hardening guide](docs/manual/hardening-security.md).

**External track (pending):**

- **Independent cryptographic review** (paid). Reviewer with FIPS 203 / FIPS 204 implementer experience preferred. Handoff package: `docs/` (specs + threat-model + ADRs) + the two internal-audit memos.
- **Side-channel review** (paid). Constant-time critical paths enumerated in the side-channel memo; verify upstream-crate assumptions, especially for `ml-dsa = "0.1.0-rc.8"` (pre-1.0).

---

## Sub-project B — FFI bindings ✅ (complete through B.6 v2)

The Rust core is exposed to platform languages via two binding paths, both built on top of a shared `secretary-ffi-bridge` crate that holds the FFI-friendly facade of `secretary-core`:

- **`secretary-ffi-bridge`** ([ffi/secretary-ffi-bridge](ffi/secretary-ffi-bridge/)): the single source of FFI code truth. Pure-safe Rust — only the binding-flavor crates need the FFI-macro `unsafe_code = "deny"` carve-out.
- **PyO3** ([ffi/secretary-ffi-py](ffi/secretary-ffi-py/)): Python bindings for the desktop / web client.
- **uniffi** ([ffi/secretary-ffi-uniffi](ffi/secretary-ffi-uniffi/)): one UDL, two outputs — Swift (iOS) and Kotlin (Android). RAII lifecycle via `with` (Python), `defer { wipe() }` (Swift), `.use { }` (Kotlin via uniffi 0.31's auto-generated `AutoCloseable`).

Phase plan (all ✅):

- **B.1 / B.1.1 / B.1.1.1** — Boilerplate proof: `add` / `version` round-trip through PyO3 + maturin, then uniffi → Swift, then uniffi → Kotlin (JVM-host runner via JNA with SHA-256-pinned `jna.jar`).
- **B.2 / B.3a** — `open_with_password` / `open_with_recovery`. First fallible + secret-bearing FFI calls. `FfiUnlockError` 5 variants preserving §13 anti-oracle conflation independently across both paths.
- **B.3b** — `create_vault`. First output-direction secret material; one-shot `MnemonicOutput::take_phrase()`; bridge instantiates `OsRng` + `Argon2idParams::V1_DEFAULT` internally so foreign callers cannot tune either knob.
- **B.4a** — `open_vault_with_password` / `open_vault_with_recovery` (folder-based). First folder-IO entry points; establishes Rust-owned folder-IO model that B.4b/c/d inherit. `OpenVaultManifest` opaque handle with read-only `block_summaries()` / `find_block()`.
- **B.4b** — `read_block`. First mutation-free block access; hybrid Record projection (non-secret metadata value-typed; secret payload via `expose_text` / `expose_bytes`).
- **B.4c** — `save_block`. First mutation path; atomic write per §9 (block-first / manifest-last); `BlockInput` / `RecordInput` / `FieldInput` input shape wraps payloads in `SecretString` / `SecretBytes` at the constructor boundary.
- **B.4d** — `share_block`. First call with `ContactCard` bytes-in; 4 typed share variants (`NotAuthor`, `RecipientAlreadyPresent`, `MissingRecipientCard`, `CardDecodeFailure`) for foreign-side UX dispatch. v1 single-author only (share-as-fork deferred).
- **B.5** — `trash_block` / `restore_block`. First lifecycle pair. `core::vault::trash_block` uses `rename(2)` to `trash/<uuid>.cbor.enc.<unix-millis>`; `restore_block` scans for largest-timestamp file and verifies + AEAD-decrypts + hybrid-verifies before rename-back. New `docs/vault-format.md` §7.1 normative sequence.
- **B.6 v1 / v2** — Cross-language conformance KAT. 26 vectors at [core/tests/data/conformance_kat.json](core/tests/data/conformance_kat.json) (open + read in v1; lifecycle — save / share / trash / restore — in v2; device-secret open + error vectors in B.2). Swift + Kotlin host runners ([ffi/secretary-ffi-uniffi/tests/{swift,kotlin}/run_conformance.sh](ffi/secretary-ffi-uniffi/tests/swift/run_conformance.sh)) both replay 27/27 PASS (26 KAT vectors + an enrol round-trip exercising the one-shot `DeviceSecretOutput` handle). Replay pins typed Ok/Err + post-call manifest shape rather than AEAD-nonce bytes (all three host runners share the same Rust bridge, so byte parity does not require nonce pinning).
- [x] **FFI projection of the record-edit primitives** (2026-06-13) — `append_record` / `edit_record` / `tombstone_record` / `resurrect_record` projected onto PyO3 + uniffi via the shared bridge (the primitives shipped bridge-only in desktop D.1.4 / D.1.5; this is the same bridge-only→projected step as the sync surface [#187](https://github.com/hherb/secretary/issues/187)), the prerequisite FFI for the native-iOS record-CRUD UI (Slice 2). A new `RecordContent` foreign input type reuses the zeroize-typed `FieldInput` / `FieldInputValue` from `save_block`; the bridge owns the CRDT semantics (untouched per-field clocks + `tombstoned_at_ms` + all `unknown` maps preserved). Per-field-clock preservation is proven at the binding boundary in Swift + Kotlin `SmokeRecordEdit` (`FieldHandle.device_uuid()`) + pyo3 `test_record_edit.py`. **No new `FfiVaultError` variant** (`BlockNotFound` / `RecordNotFound` pre-existed) → conformance stays 27/27; no `core` / on-disk-format / UDL-error / KAT change. (`read_block` surfaces tombstoned records via the per-record `tombstone()` flag — live/deleted filtering is the consumer's job.)
- [x] **pyo3 projection of the block-CRUD primitives** (2026-06-20) — `create_block` / `rename_block` / `move_record` projected onto PyO3 via the shared bridge, completing the binding parity for the block-CRUD tier (the uniffi projection shipped 2026-06-19; the pyo3 half was explicitly deferred). A new `ffi/secretary-ffi-py/src/block_crud.rs` mirrors the uniffi `namespace/block_crud.rs` and the sibling `record_edit.rs`: each op length-validates its uuid args (16 bytes → `ValueError` via `uuid_array_or_value_error`) and `move_record` additionally enforces `source_block_uuid != target_block_uuid` at the wrapper (`ValueError`) — the bridge trusts its caller on both. Error surface reuses the already-registered `VaultBlockNotFound` / `VaultRecordNotFound` / `VaultCorruptVault` + save-tail classes; **no new `FfiVaultError` variant**, so conformance stays 27/27 and there is no `core` / on-disk-format / UDL / KAT change. Proven by `ffi/secretary-ffi-py/tests/test_block_crud.py` (round-trip per op + same-block + per-arg wrong-length + absent-block/record guards).
- [x] **FFI folder-writing `create_vault_in_folder`** (2026-06-13) — the folder-writing sibling of B.3b's bytes-based `create_vault`, projected onto PyO3 + uniffi via the shared bridge. It delegates to `core::vault::create_vault` to write **all four** canonical files (so the result opens through the folder-based `open_vault_with_password`, unlike B.3b's identity-level bytes) and returns just the one-shot `MnemonicOutput` (no auto-open — the caller re-opens with the password to browse, mirroring desktop D.1.3). Requires an existing empty dir; one new typed `FfiVaultError::VaultFolderNotEmpty` distinguishes "not empty" from a wrong path / corruption, threaded through every binding + the Swift/Kotlin conformance harnesses (both stay 27/27) + desktop `map_ffi_error`. `OsRng` + `Argon2idParams::V1_DEFAULT` hardcoded bridge-side. **Slice 1 of the iOS vault create/import feature**; the native create-wizard UI is Slice 2. No `core` / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-13-ffi-folder-create-vault-design.md`](docs/superpowers/specs/2026-06-13-ffi-folder-create-vault-design.md) + [`docs/superpowers/plans/2026-06-13-ffi-folder-create-vault.md`](docs/superpowers/plans/2026-06-13-ffi-folder-create-vault.md).
- [x] **B.1 — Per-device wrap-slot format & crypto** (2026-06-10) — `devices/<uuid>.wrap`, `file_kind 0x0004`; HKDF device KEK; enroll/open/revoke; conformance KAT ([ADR 0009](docs/adr/0009-per-device-wrap-slot.md)). → B.2 #201, B.3 #202.
- [x] **B.2 — FFI projection of the per-device wrap slot** (2026-06-11, [#201](https://github.com/hherb/secretary/issues/201)) — `add_device_slot` / `open_with_device_secret` / `remove_device_slot` folder-in ops through PyO3 + uniffi via the shared bridge; a new core `Unlocker::DeviceSecret` arm makes the device open a first-class full vault open (manifest verify-before-decrypt preserved, identical to password/recovery); one-shot `DeviceSecretOutput` handle; 3 new typed `FfiVaultError` variants (`DeviceSlotNotFound` / `WrongDeviceSecretOrCorrupt` / `DeviceUuidMismatch`, wrong-length inputs → binding-layer `InvalidArgument`); 4 new conformance KAT vectors + Swift/Kotlin enrol round-trip + clean-room `conformance.py` (open cross-check + enrol round-trip).
- [x] **B.3 — iOS Secure Enclave device unlock (protocol-boundary slice)** (2026-06-11; [#202](https://github.com/hherb/secretary/issues/202) stays open for the on-device biometric proof) — pure, FFI-free `ios/SecretaryDeviceUnlock/` Swift package: `DeviceUnlockCoordinator` over three injected ports (`VaultDeviceSlotPort` / `DeviceSecretEnclave` / `DeviceEnrollmentMetadataStore`), typed `DeviceUnlockError`, host-tested via `swift test`. iOS adapters in `ios/SecretaryKit/Sources/SecretaryKit/DeviceUnlock/`: real uniffi port, non-exportable SE P-256 conformer behind biometric `SecAccessControl` + ECIES wrap, Keychain metadata store. The SE conformer compiles on the simulator and is exercised with a fake enclave; real Face ID / Touch ID on a device remains the deferred follow-up under [#202](https://github.com/hherb/secretary/issues/202). The coordinator's `unlock` funnels through the B.2 `open_with_device_secret` (same manifest verify-before-decrypt) — not a weaker open.
- [x] **iOS app walking-skeleton** ✅ 2026-06-11 — SwiftUI `SecretaryApp/` (XcodeGen) drives the full enroll / unlock / disenroll flow through the real `DeviceUnlockCoordinator`; `SecretaryDeviceUnlockUI` provides the host-tested `DeviceUnlockViewModel`; SE store records raw `domain`+`code` diagnostic for [#202](https://github.com/hherb/secretary/issues/202). Built via `ios/scripts/build-app.sh`, included in `ios/scripts/run-ios-tests.sh`. On-device biometric proof ([#202](https://github.com/hherb/secretary/issues/202)) ✅ **verified on an iPhone 13 Pro Max** (2026-06-11): real SE + Face ID released the device secret and opened the vault (uuid matched the pinned fixture); biometric cancel/non-match arrive in `LAError` (`userCancel`) → typed `userCancelled` (the `SecKey`-triggered eval funnels failures to `userCancel`; distinct `authenticationFailed`/`biometryLockout` are defensive). No failure produced `wrappedSecretCorrupt` — the "never mislabel auth as tamper" property held on hardware; no `mapDecryptError` change needed.
- [x] **iOS app — password/recovery unlock + read-only browse** ✅ 2026-06-12 — a new FFI-free `ios/SecretaryVaultAccess/` Swift package (ports `VaultOpenPort`/`VaultSession`, pure models, typed `VaultAccessError`, and host-tested `UnlockViewModel` / `VaultBrowseViewModel`) opens the staged vault by **password or 24-word recovery phrase** and browses blocks → records read-only with **reveal-on-demand** secret fields; real `UniffiVaultOpenPort` / `UniffiVaultSession` adapters in `ios/SecretaryKit/Sources/SecretaryKit/VaultAccess/` over the already-projected `open_vault_with_password`/`recovery` + `read_block`, with a simulator integration test against golden_vault_001. The error mapping preserves the core's §13 anti-oracle conflation (a wrong credential is indistinguishable from corruption); reveals drop + the session wipes on background (the VM's authoritative `lock()`). 100% Swift — no Rust / on-disk-format / FFI-surface change. Spec + plan in [`docs/superpowers/specs/2026-06-12-ios-vault-unlock-browse-design.md`](docs/superpowers/specs/2026-06-12-ios-vault-unlock-browse-design.md) + [`docs/superpowers/plans/2026-06-12-ios-vault-unlock-browse.md`](docs/superpowers/plans/2026-06-12-ios-vault-unlock-browse.md).
- [x] **iOS app — vault selection** ✅ 2026-06-12 — the app opens a **user-selected vault folder** (system `.fileImporter`) and **remembers it across launches** via a persisted security-scoped bookmark, replacing the hardcoded staged vault + prefilled demo password (the bundled golden vault stays as an explicit opt-in demo). A pure, FFI-free `VaultLocationStore` port + a host-tested `VaultSelectionViewModel` (empty/located/unavailable state machine) live in `ios/SecretaryVaultAccess/`; a single-owner `ScopedVaultPath` holds the OS security scope for the whole session (lazy block reads) and releases it exactly once on lock; the real `BookmarkVaultLocationStore` (bookmark persistence in `UserDefaults` + scoped access, stale-bookmark refresh) is in `ios/SecretaryKit/Sources/SecretaryKit/VaultAccess/`, with a simulator test that opens golden_vault_001 through a resolved bookmark. Stale/unresolvable bookmarks surface as typed errors (no silent fallback); the begin/end scope balance is unit-tested. 100% Swift — no Rust / on-disk-format / FFI-surface change. On-device smoke ✅ **verified on an iPhone 13 Pro Max** (2026-06-12): side-loaded vault → pick → persisted bookmark survived relaunch → unlock by password and recovery → browse → reveal → background re-lock. Spec + plan in [`docs/superpowers/specs/2026-06-12-ios-vault-selection-design.md`](docs/superpowers/specs/2026-06-12-ios-vault-selection-design.md) + [`docs/superpowers/plans/2026-06-12-ios-vault-selection.md`](docs/superpowers/plans/2026-06-12-ios-vault-selection.md).
- [x] **iOS app — record CRUD (D.3 Slice 2)** ✅ 2026-06-13 — on a selected, unlocked vault the app can **add** a new record (type + tags + text/bytes fields), **edit** an existing record's full content (add/remove/rename fields, switch text↔bytes, edit values, change type+tags — CRDT-correct via the bridge edit primitives; per-field clocks, `tombstoned_at_ms`, and all `unknown` maps preserved by the bridge), **soft-delete** (tombstone) and **restore** (resurrect) a record. Deleted records are hidden by default behind a "Show deleted" toggle (filtered client-side in Swift). The per-field CRDT modifier clock uses a stable per-(install, vault) device UUID persisted in Application Support (desktop parity). Verified by host-tested `RecordEditViewModel` + a simulator XCTest add/edit/delete/restore round-trip incl. a cross-open durability check against a temp copy of golden_vault_001. 100% Swift — no Rust / on-disk-format / FFI-surface change (the record-edit bridge primitives were projected in the preceding FFI slice). **Deferred follow-ups:** `include_deleted` Rust gate (currently filtered client-side in Swift), iOS vault create/import. (Biometric re-auth before a write ✅ shipped 2026-06-21 — see below.)
- [x] **iOS app — vault create / import** ✅ 2026-06-14 — the selection screen branches **Create new vault** / **Import existing vault**. Create is a 3-step wizard (pick a parent folder + vault name → master password + confirm + display name → 24-word recovery-phrase screen gated by an "I wrote it down" acknowledgement) that mkdir's a fresh subfolder inside the picked parent and writes the vault via the Slice-1 `createVaultInFolder` FFI, then routes back to select so the user re-enters the password to open (desktop D.1.3 parity — no auto-open, shortest secret lifetime). Import adds a crypto-free `vault.toml` shape probe so a non-vault folder is rejected with a clear message before any password entry. The wizard logic is pure + host-tested: `VaultProvisioningViewModel` over `VaultCreatePort` / `VaultShapeProbe` plus pure helpers (`validateVaultName` / `passwordsMatch` / `groupMnemonic`) in `ios/SecretaryVaultAccess/`; the real `UniffiVaultCreatePort` (security-scoped mkdir + child-URL bookmark + one-shot phrase) / `FileManagerVaultShapeProbe` live in `ios/SecretaryKit/`, with a simulator create→open round-trip in a tempdir (never the golden fixture). The new-vault location is persisted **before** the mnemonic screen (crash mid-flow leaves an openable vault); the recovery phrase is never carried in the `Equatable` step enum and is scrubbed on acknowledge. 100% Swift — no Rust / on-disk-format / FFI-surface change. Follow-up [#224](https://github.com/hherb/secretary/issues/224) — host RootView route VMs as `@StateObject` so a scenePhase toggle doesn't reset wizard state. Spec + plan in [`docs/superpowers/specs/2026-06-14-ios-vault-create-import-design.md`](docs/superpowers/specs/2026-06-14-ios-vault-create-import-design.md) + [`docs/superpowers/plans/2026-06-14-ios-vault-create-import.md`](docs/superpowers/plans/2026-06-14-ios-vault-create-import.md).
- [x] **iOS app — Argon2id off the main actor** ✅ 2026-06-14 — the CPU-heavy password/recovery **open** and **create** KDF (m=256 MiB, t=3) no longer blocks the UI. `VaultOpenPort.openWith{Password,Recovery}` and `VaultCreatePort.create` became `async throws`; the real `UniffiVaultOpenPort` / `UniffiVaultCreatePort` adapters offload the synchronous FFI call through a shared `runOffMainActor` helper (`withCheckedThrowingContinuation` + a `DispatchQueue.global(qos: .userInitiated)` hop — chosen over `Task.detached` so the non-`Sendable` `VaultSession` return crosses back without a Swift-5.9 Sendable warning), so the `@MainActor` `UnlockViewModel` / `VaultProvisioningViewModel` **suspend rather than block** during the derivation. Device unlock is untouched (HKDF, already fast). The pure view-models keep their full state machines (`.busy`→`.unlocked`/`.failed`; persist-before-mnemonic), gaining only `await`; a new `SuspensionGate` test actor lets host tests hold a fake port mid-call and prove the main actor is free (would deadlock against the old synchronous code). 100% Swift — no Rust / on-disk-format / FFI-surface change. Spec + plan in [`docs/superpowers/specs/2026-06-14-argon2id-off-main-actor-design.md`](docs/superpowers/specs/2026-06-14-argon2id-off-main-actor-design.md) + [`docs/superpowers/plans/2026-06-14-argon2id-off-main-actor.md`](docs/superpowers/plans/2026-06-14-argon2id-off-main-actor.md).
- [x] **iOS app — sync orchestration core (C.3 slice 1)** ✅ 2026-06-15 — pure host-tested Swift over the existing `sync_status` / `sync_vault` / `sync_commit_decisions` uniffi surface ([#187](https://github.com/hherb/secretary/issues/187)): metadata-only value types + `VaultSyncPort` + a dedicated `VaultSyncError` (separate from `VaultAccessError`, §13 anti-oracle conflation preserved) + a `SyncCoordinator` actor threading the two-call inspect→commit round-trip (the `manifest_hash` freshness token held privately; password passed per call, never stored), all in `ios/SecretaryVaultAccess/`; the real `UniffiVaultSyncPort` in `ios/SecretaryKit/` offloads the Argon2id-bearing `sync` / `commitDecisions` off the main actor via `runOffMainActor` (status inline). Host-tested coordinator round-trip + simulator off-main-actor responsiveness tests (sync **and** commit). Manual/programmatic trigger only — file-change detection + sync UI are later C.3 slices. 100% Swift — no Rust / on-disk-format / FFI-surface change. See the C.3 phase plan below. Spec + plan in [`docs/superpowers/specs/2026-06-14-c3-ios-sync-orchestration-core-design.md`](docs/superpowers/specs/2026-06-14-c3-ios-sync-orchestration-core-design.md) + [`docs/superpowers/plans/2026-06-14-c3-ios-sync-orchestration-core.md`](docs/superpowers/plans/2026-06-14-c3-ios-sync-orchestration-core.md).
- [x] **iOS app — folder-change detection + sync UI (C.3 slices 2–3)** ✅ 2026-06-15 — slice 2 added an advisory, detect-only debounced + foreground-gated "remote changes detected" signal (`pendingChanges`) for an open vault's folder; slice 3 made the whole C.3 stack **user-visible** — a sync-status badge on the browse screen, an opportunistic **sync-at-unlock**, an on-demand re-prompt sync, and a metadata-only **conflict-resolution sheet** (per-record Keep mine / Accept delete, default Keep mine) mirroring desktop D.1.15. Two triggers funnel into one interactive resolution path so the password is never held across a modal at unlock. The two deferred decisions are resolved: **state dir = app-sandbox Application Support**; **password policy = sync-at-unlock + re-prompt**. Pure host-tested `VaultSyncViewModel` + `SyncBadgeState` + `WallClock`/`SyncMonitorHook` seams in `ios/SecretaryVaultAccess/` + thin SwiftUI views + `SecretaryKit` conformers (`SystemWallClock`, `MonitorSyncHook`, `defaultSyncStateDir`, `makeVaultSync`). 100% Swift — no Rust / on-disk-format / FFI-surface change. See the C.3 phase plan below.
- [x] **iOS app — biometric re-auth before a write** ✅ 2026-06-21 — every mutating vault write (add/edit/delete/restore record, move record, create/rename block) prompts a Face ID / Touch ID re-auth **before** the write, throttled by a **grace window** (`ReauthWindow.v1Default = 30s` since the last successful auth, so a burst of edits costs one prompt). The gate reuses the **same Secure-Enclave key-release** as device unlock (`DeviceSecretEnclave.release`; the released device secret is zeroized + discarded — strictly stronger than a bare `LAContext.evaluatePolicy` presence check) and engages only when device-unlock is enrolled (`enclave.isEnrolled`); a non-enrolled session writes exactly as before (no SE key to release, no regression). Pure FFI-free core in `ios/SecretaryVaultAccess/` — `WriteReauthGate` / `BiometricAuthorizer` ports + the pure `needsReauth(lastAuthAt:now:window:)` policy (boundary inclusive) + a `@MainActor GraceWindowReauthGate` holder seeded at unlock; both `@MainActor` view models (`RecordEditViewModel.commit`, the four `VaultBrowseViewModel` writes) became `async` and `await` the gate after input validation, before the write — a refused biometric surfaces a new `VaultAccessError.reauthFailed` (Swift-only enum; no `FfiVaultError`/UDL/conformance change) and leaves any open dialog/sheet open, writing nothing. Real `EnclaveBiometricAuthorizer` in `ios/SecretaryKit/`; production wiring builds the gate over `SecureEnclaveDeviceSecretStore`. Host-tested gate/policy + VM gating tests + a simulator round-trip over a real not-enrolled gate; on-device Face ID is a manual checklist item ([#202](https://github.com/hherb/secretary/issues/202) parity). 100% Swift — no Rust / on-disk-format / FFI-surface change. Spec + plan in [`docs/superpowers/specs/2026-06-20-ios-write-reauth-design.md`](docs/superpowers/specs/2026-06-20-ios-write-reauth-design.md) + [`docs/superpowers/plans/2026-06-20-ios-write-reauth.md`](docs/superpowers/plans/2026-06-20-ios-write-reauth.md). **Hardened 2026-06-21 ([#282](https://github.com/hherb/secretary/issues/282)):** the grace-window clock is now a **monotonic** source (the existing `MonotonicInstant` / `MonotonicInstant.now` over `DispatchTime` uptime — injected from the composition root, the same source the folder-change detector uses), not wall-clock `Date`; an elapsed-time gate must not move under an NTP correction or a user clock-set (which on a wall clock can jump backward and extend the silent window). Parity with Android #281.
- [x] **Android app — biometric re-auth before a write** ✅ 2026-06-21 — every mutating vault write (add/edit/delete/restore record, move record, create/rename block) requires a biometric presence proof **before** the write, throttled by a **30 s grace window** (one prompt covers a burst of edits). The gate reuses `KeystoreDeviceSecretEnclave.release` — the same `BiometricPrompt`/`CryptoObject` path that backs device unlock (stronger than a bare `BiometricPrompt.authenticate`) — and engages only when device-unlock is enrolled (`isEnrolled`); a password-only / non-enrolled session writes exactly as before. Pure Kotlin in `android/vault-access` (`:vault-access`) — `WriteReauthGate` / `BiometricAuthorizer` ports + `GraceWindowReauthGate` (window-keyed, seeded at unlock, reset on lock) + pure `needsReauth` policy + `NoopReauthGate` for non-enrolled sessions — and real `CoordinatorBiometricAuthorizer` in `:vault-access` (wraps `KeystoreDeviceSecretEnclave` + `BiometricPromptGate`). Both `VaultBrowseModel` and `RecordEditModel` call `guardedWrite(reason, …)` / `commitThenReload(reason, …)` with a typed human-readable reason string before every vault mutation; a `BiometricPrompt` cancel is silent (dialog stays open, nothing written); any other failure surfaces `VaultBrowseError.ReauthFailed(detail)` — leaving the open dialog intact and writing nothing. `AppRoot` seeds the gate at vault open and passes it into both VMs. Host-tested (pure policy, gate, VM gating); real `BiometricPrompt` path covered by the existing device-open instrumented tests; on-device fingerprint/face is a manual checklist item. Android-only; no `core` / `ffi` / on-disk-format change. **Deferred follow-ups:** configurable/persisted grace-window duration; presence proof for password-only sessions with no device-secret enrollment. Spec + plan in [`docs/superpowers/specs/2026-06-21-android-write-reauth-design.md`](docs/superpowers/specs/2026-06-21-android-write-reauth-design.md) + [`docs/superpowers/plans/2026-06-21-android-write-reauth.md`](docs/superpowers/plans/2026-06-21-android-write-reauth.md).
- [x] **Android app — sync orchestration core (C.3 slice 1)** ✅ 2026-06-15 — pure host-tested Kotlin: metadata-only value types + `VaultSyncPort` + a dedicated `VaultSyncError` (separate from vault-access, §13 anti-oracle preserved) + a `Mutex`-guarded `SyncCoordinator` threading the two-call inspect→commit round-trip (freshness token held privately; password passed per call, never stored), all in `android/vault-access/`. The repo's first Gradle project — a `kotlin("jvm")` module host-tested on JUnit5 + coroutines-test (no emulator/NDK). The real `UniffiVaultSyncPort` adapter, folder-change detection (SAF + `WorkManager`), and Compose UI are later slices. 100% Kotlin — no Rust / on-disk-format / FFI-surface change. Spec + plan in [`docs/superpowers/specs/2026-06-15-c3-android-sync-orchestration-core-design.md`](docs/superpowers/specs/2026-06-15-c3-android-sync-orchestration-core-design.md) + [`docs/superpowers/plans/2026-06-15-c3-android-sync-orchestration-core.md`](docs/superpowers/plans/2026-06-15-c3-android-sync-orchestration-core.md).
- [x] **Android app — real `UniffiVaultSyncPort` adapter (C.3 slice 2a)** ✅ 2026-06-15 — a new `:kit` Android-library module (`org.secretary.sync`) hosting the real `UniffiVaultSyncPort` over the generated uniffi Kotlin bindings + arm64 `jniLibs` (cross-built via cargo-ndk at build time), with the pure DTO→domain + `VaultException`→`VaultSyncError` mappers. Host-tested (mappers + adapter with injected seams) and build-verified — the arm64 `libsecretary_ffi_uniffi.so` packs into the release AAR. The emulator round-trip against the golden vault is slice 2b. No Rust / on-disk-format / FFI-surface change.
- [x] **Android app — emulator sync round-trip (C.3 slice 2b)** ✅ 2026-06-16 — an instrumented (`connectedDebugAndroidTest`) test drives `golden_vault_001` through `status` / `sync` over the real native `.so` via `UniffiVaultSyncPort` + `SyncCoordinator` on the `Medium_Phone_API_36.1` arm64 emulator, proving the adapter on-device. The host path stays NDK-free (`:vault-access` + `:kit` unit tests). Folder-change detection + Compose UI are later slices. No Rust / on-disk-format / FFI-surface change.
- [x] **Android app — folder-change detection (C.3 slice 3)** ✅ 2026-06-16 — the Android mirror of iOS slice 2: an advisory, **detect-only** debounced + foreground-gated "remote changes detected" signal (`pendingChanges`) for an open vault's folder. A pure FFI-free core in `:vault-access` (a `FolderChangeDetector` trailing-debounce reducer over caller-supplied `MonotonicInstant`s + a `ChangeDetectionMonitor` over two injected ports `FolderWatchPort`/`FlushScheduler`) fully host-tested via fakes; the real adapters in `:kit` are a single **non-recursive** `android.os.FileObserver` on the vault **root** (`FileObserverFolderWatch`) + a main-`Looper` `HandlerFlushScheduler`, proven by one emulator smoke. Root-only watching is correct because `manifest.cbor.enc` (top-level) is rewritten on every committed state advance — `FileObserver` is non-recursive on all API levels, so a per-subdir tree was deliberately avoided. **Foreground-only** per [ADR-0003](docs/adr/0003-cloud-folder-sync.md); `WorkManager` background polling is documented as deferred (Doze defers it; SAF `content://` URIs aren't readable by the path-based FFI anyway). Detect-only: no `runPass`, no `ViewModel` yet — slice 4 (Compose UI) consumes the signal. 100% additive Kotlin — no Rust / FFI-surface / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-16-c3-android-folder-change-detection-design.md`](docs/superpowers/specs/2026-06-16-c3-android-folder-change-detection-design.md) + [`docs/superpowers/plans/2026-06-16-c3-android-folder-change-detection.md`](docs/superpowers/plans/2026-06-16-c3-android-folder-change-detection.md).
- [x] **Android app — sync-UI model (C.3 slice 4)** ✅ 2026-06-16 — the host-tested heart of the Android sync UI, the Android mirror of iOS slice 3 minus rendering. Pure FFI-free Kotlin in `:vault-access`: `syncBadgeState` (5-state precedence), `VaultSyncModel` (StateFlow surface; two triggers — silent `syncAtUnlock` + interactive `runInteractivePass`/`resolve` — on one resolution path; metadata-only conflict surface; typed errors keep the conflict context for retry; `acknowledge` only on clean arms), the `WallClock`/`SyncMonitorHook` seams, and `collectDecisions`/`decisionsComplete` (default "Keep mine"), all JUnit5-host-tested via fakes. Real `:kit` wiring: `SystemWallClock`, `MonitorSyncHook`, and a `makeVaultSync` factory. The Compose render is slice 5. 100% additive Kotlin — no Rust / FFI-surface / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-16-c3-android-sync-ui-design.md`](docs/superpowers/specs/2026-06-16-c3-android-sync-ui-design.md) + [`docs/superpowers/plans/2026-06-16-c3-android-sync-ui-model.md`](docs/superpowers/plans/2026-06-16-c3-android-sync-ui-model.md).

Specs and per-phase plans live under [docs/superpowers/specs/](docs/superpowers/specs/) and [docs/superpowers/plans/](docs/superpowers/plans/).

---

## Sub-project C — Sync orchestration 🚧 (C.1 + C.1.1a/b + C.2 ✅; C.3 iOS shipped — orchestration core + folder detection + sync UI; C.3 Android orchestration core + real adapter + emulator round-trip + folder-watch + sync-UI model + Compose render + `:app` skeleton + open/browse + reveal-on-tap + soft-delete + biometric write re-auth ✅; C.4 convergence conformance ✅)

This is the layer that turns "the Rust core knows how to merge two manifests" into "two devices sharing a cloud folder converge on the same vault state without user intervention". It sits between the FFI and the platform UIs as its own phase rather than being folded into UI work, so the orchestration logic is built and tested once with a headless reference consumer instead of being re-invented per platform.

Scope:

- **File watching**: cross-platform via the `notify` crate on desktop; per-platform shims on iOS (`NSFilePresenter` / `NSMetadataQuery`) and Android (Storage Access Framework). The state machine that consumes events is the same on every platform.
- **Cloud-folder integration**: wait for the cloud-folder client (iCloud, Drive, Dropbox, OneDrive, WebDAV) to mark a file as fully downloaded before reading it (ADR-0003).
- **Conflict-detection scheduling**: when manifest fingerprints diverge between local and remote, invoke the Sub-project A merge primitives, persist the merged manifest atomically, surface unresolvable conflicts to the UI.
- **Retry / backoff / power-and-network awareness**: especially on mobile.

Phase plan:

- **C.1 — Sync state machine (pure Rust)** ✅. No OS dependencies; property-tested for convergence under random event interleavings.
  - **C.1 phase 1 — sync detection** ✅ — `core::sync::sync_once(folder, &UnlockedIdentity, &SyncState, now_ms)` exposes the §10 rollback-and-fork-detection algorithm as a pure-function dispatch over `clock_relation`. `SyncState` is caller-persisted (canonical-CBOR, sorted/deduped invariant validated symmetrically by constructor and decoder). New `core::vault::read_vault_manifest` lets a sync poll reuse a pre-unlocked identity without re-running Argon2.
  - **C.1.1a — conflict-copy ingestion** ✅ — `SyncOutcome::ConcurrentDetected { bundle, plan, manifest_hash, ... }`. Scans the vault folder for sibling `*.cbor.enc` files (Dropbox / iCloud / Syncthing / OneDrive naming all keep the canonical prefix), authenticates each per spec §1a-D4's five MUST rules (decode + hybrid signature + canonical vault_uuid + canonical owner fingerprint + AEAD-decrypts under the unlocked IBK), and packages canonical + N authenticated copies into a `VaultBundle`. Failures silently drop per spec §1a-D3. New public `core::vault::verify_block_signature`. Quiet-vault fast path pays zero scan cost.
  - **C.1.1b — sync merge layer** ✅ — three-step merge API: `sync_once → prepare_merge → commit_with_decisions`. `prepare_merge` folds diverging-block siblings through `merge_block` and returns a `DraftMerge` with `Vec<RecordTombstoneVeto>` for live-edit-vs-tombstone pairs. `commit_with_decisions` enforces the `vetoes ↔ decisions` bijection (typed `MissingVetoDecision` / `UnknownVetoDecision`), re-checks captured `manifest_hash` for TOCTOU (typed `EvidenceStale` with zero writes on miss), and writes block-first / manifest-last under §9. Partial-commit crash recovery proven by `verify_block_fingerprints` + CRDT idempotence; no orchestrator-side journaling needed. Four merge-layer proptests pin the math (post-commit fixpoint, deterministic merge, decision-order independence, bijection enforcement). 16-vector KAT at [`core/tests/data/sync_kat.json`](core/tests/data/sync_kat.json).
- **C.2 — Headless `secretary-sync` CLI (desktop)** ✅ — wraps the C.1 state machine + the `notify` crate + Sub-project A. Doubles as the reference consumer for testing and as a real user-facing tool for headless deployments (NAS, server). Spec + 10-task plan in [`docs/superpowers/plans/2026-05-23-c2-headless-sync-cli.md`](docs/superpowers/plans/2026-05-23-c2-headless-sync-cli.md); landed across 10 PRs (#112 scaffold + exit codes, #114 state + lockfile, #115 password sourcing, #116 veto UX, #118 pipeline + lib/bin split, #119 watcher ready + debounce, #121 notify driver + daemon loop, #124 logging + signal handling, #125 `main.rs` + `once` integration suite, plus Task 10's two-instance convergence + cross-platform `notify` quirk pin + this README/ROADMAP sweep). `secretary-sync once <folder>` runs end-to-end against a real vault; `secretary-sync run <folder>` runs the daemon loop with `notify`-driven trailing-edge debounce, periodic-poll safety net, partial-download size-stability gate, and SIGINT/SIGTERM-driven clean shutdown. Convergence across two daemons on the same vault is pinned in CI ([`cli/tests/two_instance_convergence.rs`](cli/tests/two_instance_convergence.rs)); cross-platform `notify` event semantics are pinned in CI ([`cli/tests/notify_quirk.rs`](cli/tests/notify_quirk.rs)).
- **C.3 — Mobile sync adapters** 🚧. iOS adapter using `NSFilePresenter` / `NSMetadataQuery`; Android adapter using a foreground `android.os.FileObserver` (the originally-planned SAF + `WorkManager` background path was deferred — see slice 3). Both expose the C.1 state machine via uniffi.
  - **C.3 slice 1 — iOS sync orchestration core** ✅ 2026-06-15 — pure host-tested Swift over the existing `sync_status` / `sync_vault` / `sync_commit_decisions` uniffi surface (#187): metadata-only value types + `VaultSyncPort` + a dedicated `VaultSyncError` (separate from `VaultAccessError`, anti-oracle conflation preserved) + a `SyncCoordinator` actor threading the two-call inspect→commit round-trip (the `manifest_hash` freshness token held privately; password passed per call, never stored), all in `SecretaryVaultAccess`; the real `UniffiVaultSyncPort` in `SecretaryKit` offloads the Argon2id-bearing `sync` / `commitDecisions` off the main actor via `runOffMainActor` (status inline). Manual/programmatic trigger only — no file-change detection, no UI yet. 100% Swift — no Rust / FFI-surface / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-14-c3-ios-sync-orchestration-core-design.md`](docs/superpowers/specs/2026-06-14-c3-ios-sync-orchestration-core-design.md) + [`docs/superpowers/plans/2026-06-14-c3-ios-sync-orchestration-core.md`](docs/superpowers/plans/2026-06-14-c3-ios-sync-orchestration-core.md).
  - **C.3 slice 2 — iOS folder-change detection** ✅ 2026-06-15 — an advisory, **detect-only** debounced + foreground-gated "remote changes detected" signal (`pendingChanges`) for an open vault's folder. Detect-only because `sync_vault` needs the password (full Argon2id), which the app drops after unlock — a file event can't silently run a pass, so it sets a flag a later UI slice acts on (re-prompt / sync-at-unlock decided in slice 3). Pure FFI-free core in `SecretaryVaultAccess` (a `FolderChangeDetector` trailing-debounce reducer over caller-supplied `MonotonicInstant`s + a `ChangeDetectionMonitor` over two injected ports `FolderWatchPort`/`FlushScheduler`, plus a self-write mute hook) fully host-tested via fakes; real `PresenterFolderWatch` (`NSFilePresenter`, callbacks confined to the main queue) + `DispatchFlushScheduler` in `SecretaryKit` with one simulator smoke test. Foreground-only per ADR-0003. 100% Swift — no Rust / FFI-surface / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-15-c3-ios-folder-change-detection-design.md`](docs/superpowers/specs/2026-06-15-c3-ios-folder-change-detection-design.md) + [`docs/superpowers/plans/2026-06-15-c3-ios-folder-change-detection.md`](docs/superpowers/plans/2026-06-15-c3-ios-folder-change-detection.md).
  - **C.3 slice 3 — iOS sync UI** ✅ 2026-06-15 — makes slices 1+2 user-visible: a sync-status badge on the browse screen (over `pendingChanges` + `SyncStatus`), an opportunistic **sync-at-unlock**, an on-demand re-prompt sync, and a metadata-only **conflict-resolution sheet** mirroring desktop D.1.15 (per-record Keep mine / Accept delete, default Keep mine, read-only auto-merged-collision disclosure). Two triggers funnel into one interactive resolution path, so the password is never held across a modal at unlock (a conflict detected at unlock just flips the badge to "review needed"; resolution re-prompts). Resolves the two deferred decisions: **state dir = app-sandbox Application Support** (`secretary/sync/`) and **password policy = sync-at-unlock + re-prompt** (sync needs the full password, dropped after unlock; the device-secret path can't feed it without an FFI change). Pure host-tested core in `SecretaryVaultAccess`/`SecretaryVaultAccessUI` (a `WallClock` port, `SyncBadgeState` derivation, a `SyncMonitorHook` seam, the `VaultSyncViewModel`) + thin SwiftUI views (`SyncBadgeView`/`SyncPasswordSheet`/`ConflictResolutionSheet`) + `SecretaryKit` conformers (`SystemWallClock`, `MonitorSyncHook`, `defaultSyncStateDir`, `makeVaultSync`); self-write mute wired around sync's own commits. 100% Swift — no Rust / FFI-surface / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-15-c3-ios-sync-ui-design.md`](docs/superpowers/specs/2026-06-15-c3-ios-sync-ui-design.md) + [`docs/superpowers/plans/2026-06-15-c3-ios-sync-ui.md`](docs/superpowers/plans/2026-06-15-c3-ios-sync-ui.md).
  - **C.3 slice 1 (Android) — Android sync orchestration core** ✅ 2026-06-15 — the Android mirror of the iOS slice-1 orchestration core, pure host-tested Kotlin in `android/vault-access/` (the repo's first Gradle project, a `kotlin("jvm")` module): metadata-only value types + `VaultSyncPort` + a dedicated `VaultSyncError` (separate from vault-access, §13 anti-oracle conflation preserved) + a `Mutex`-guarded `SyncCoordinator` threading the two-call inspect→commit round-trip (the `manifest_hash` freshness token held privately; password passed per call, never stored). Host-tested on JUnit5 + coroutines-test (no emulator/NDK). The real `UniffiVaultSyncPort` adapter, folder-change detection (SAF + `WorkManager`), and Compose UI are later slices. 100% Kotlin — no Rust / FFI-surface / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-15-c3-android-sync-orchestration-core-design.md`](docs/superpowers/specs/2026-06-15-c3-android-sync-orchestration-core-design.md) + [`docs/superpowers/plans/2026-06-15-c3-android-sync-orchestration-core.md`](docs/superpowers/plans/2026-06-15-c3-android-sync-orchestration-core.md).
  - **C.3 slice 2a (Android) — real `UniffiVaultSyncPort` adapter** ✅ 2026-06-15 — a new `:kit` Android-library module (`org.secretary.sync`) hosting the real `UniffiVaultSyncPort` over the generated uniffi Kotlin bindings + arm64 `jniLibs` (cross-built via cargo-ndk at build time), plus the pure DTO→domain + `VaultException`→`VaultSyncError` mappers. Host-tested (mappers + adapter with injected seams) and build-verified — the arm64 `libsecretary_ffi_uniffi.so` packs into the release AAR. No Rust / FFI-surface / on-disk-format change.
  - **C.3 slice 2b (Android) — emulator round-trip** ✅ 2026-06-16 — an instrumented test drives `golden_vault_001` through `status` / `sync` over the real native `.so` via `UniffiVaultSyncPort` + `SyncCoordinator` on the `Medium_Phone_API_36.1` emulator. Folder-change detection + Compose UI are later slices. No Rust / FFI-surface / on-disk-format change.
  - **C.3 slice 3 (Android) — folder-change detection** ✅ 2026-06-16 — the Android mirror of iOS slice 2: an advisory, **detect-only** debounced + foreground-gated `pendingChanges` signal for an open vault's folder. Pure FFI-free core in `:vault-access` (a `FolderChangeDetector` trailing-debounce reducer over caller-supplied `MonotonicInstant`s + a `ChangeDetectionMonitor` over injected `FolderWatchPort`/`FlushScheduler` ports) fully host-tested via fakes; real adapters in `:kit` are a single **non-recursive** `android.os.FileObserver` on the vault **root** (`FileObserverFolderWatch`) + a main-`Looper` `HandlerFlushScheduler`, with one emulator smoke. Root-only is correct (the top-level `manifest.cbor.enc` is rewritten on every committed advance; `FileObserver` is non-recursive on all APIs, so a per-subdir tree was avoided). **Foreground-only** per ADR-0003; `WorkManager` background polling documented as deferred. Detect-only — no `runPass`, no `ViewModel` yet (slice 4 consumes the signal). 100% additive Kotlin — no Rust / FFI-surface / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-16-c3-android-folder-change-detection-design.md`](docs/superpowers/specs/2026-06-16-c3-android-folder-change-detection-design.md) + [`docs/superpowers/plans/2026-06-16-c3-android-folder-change-detection.md`](docs/superpowers/plans/2026-06-16-c3-android-folder-change-detection.md).
  - **C.3 slice 4 (Android) — sync-UI model** ✅ 2026-06-16 — the host-tested heart (badge state + `VaultSyncModel` two-trigger/one-resolution state machine + `WallClock`/`SyncMonitorHook` seams + decision helpers) in `:vault-access`, plus real `:kit` wiring (`SystemWallClock`, `MonitorSyncHook`, `makeVaultSync`). Mirrors iOS slice 3 minus rendering; the Compose render is slice 5. No Rust / FFI-surface / on-disk-format change.
  - **C.3 slice 5 (Android) — Compose sync render** ✅ 2026-06-16 — a new FFI-free `:sync-ui` Compose Android library module (namespace `org.secretary.sync.ui`, depends on `:vault-access` only) renders the slice-4 `VaultSyncModel`. `VaultSyncViewModel` is a thin `androidx.lifecycle.ViewModel` bridge re-exposing the model's StateFlows and owning the password-sheet visibility flag (host JUnit5 tested). Three hoisted Compose surfaces: `SyncBadge` (5-state badge with spinner while syncing and a relative "synced N ago" label), `SyncPasswordSheet` (ModalBottomSheet; password held in transient Compose state only, never persisted, stays open on error), and `ConflictResolutionSheet` (metadata-only per-record Keep mine / Accept delete, default Keep mine, read-only auto-merged-collision summary — mirrors desktop D.1.15); plus `SyncScreen` wiring them together. Pure render helpers (`relativeSyncedLabel`, `badgeLabel`, `badgeIcon`, `syncErrorLabel`) host-tested; icons from `material-icons-core` (no heavy extended dep). The interactive password is held only in `SyncScreen`'s transient Compose state and zeroized (`fill(0)`) on every terminal path (including retry); never stored on the VM or model. **First Compose UI-test harness on Android**: 15 instrumented Compose UI tests on the emulator (fake-backed `VaultSyncModel` — no native `.so` needed), plus host JUnit5 for helpers and the ViewModel. Compose BOM bumped to 2025.05.00 + Espresso forced to 3.7.0 for API-36 emulator compat. App lifecycle wiring (real `makeVaultSync` into an unlock/lock flow) shipped in the `:app` module (slice 6). 100% additive Kotlin — no Rust / FFI-surface / on-disk-format change.
  - **C.3 slice 6 (Android) — `:app` walking skeleton** ✅ 2026-06-17 — the first runnable Android app: a `com.android.application` `:app` module hosting the slice-5 `SyncScreen` over the **real** `makeVaultSync` lifecycle. Unlock → silent `syncAtUnlock` → badge, monitor lifecycle bound to the Sync screen's composition, `FLAG_SECURE`, bundled writable `golden_vault_001` demo vault; proven by an on-device `MakeVaultSyncSmokeTest`. Sync-only at ship; vault open/browse followed in slice 7. 100% additive Kotlin — no Rust / FFI-surface / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-17-c3-android-app-skeleton-design.md`](docs/superpowers/specs/2026-06-17-c3-android-app-skeleton-design.md).
  - **C.3 slice 7 (Android) — vault open/browse** ✅ 2026-06-17 — the first slice where Android **opens** a vault. Unlock → real `open_vault_with_password` (Argon2id offloaded to IO dispatcher inside `UniffiVaultOpenPort`) → a metadata-only Compose `BrowseScreen` listing blocks and, for a selected block, record titles/types/tags/field-names. No secret value crosses the adapter (`expose_*` never called; `BlockReadOutput` closed immediately). A new FFI-free `:browse-ui` module houses `VaultBrowseViewModel` + the Compose render. Lock-on-background wipes the `VaultSession` and returns to Unlock. The prior sync flow stays in the repo; sync-badge re-integration onto `BrowseScreen` + reveal-on-tap are the next slices. The open/read uniffi surface already existed from iOS — this is a pure Kotlin-port + Compose-UI slice with no `core` / `ffi` / `ios` / on-disk-format change. Host-tested throughout; proven on-device by `OpenBrowseSmokeTest` (2 cases) + `MakeVaultSyncSmokeTest` (2 cases), 4/4 on `Medium_Phone_API_36.1`. Spec + plan in [`docs/superpowers/specs/2026-06-17-c3-android-open-browse-design.md`](docs/superpowers/specs/2026-06-17-c3-android-open-browse-design.md).
  - **C.3 slice 8 (Android) — reveal-on-tap** ✅ 2026-06-17 — the first Android slice where a secret value crosses the adapter: tapping a field materializes that one value via the FFI `expose_text`/`expose_bytes` through a retained `BlockReadOutput`, with 30s auto-hide, tap-to-hide, and drop-on-background. The only `expose_*` call sites live in one reveal lambda; `wipe()` zeroizes blocks → manifest → identity. `VaultBrowseModel` owns a `revealed` map keyed `recordUuidHex/fieldName`, cleared on every reload/lock; an unexpected throwable from a field lambda folds to a typed error rather than crashing. Host-tested (`:vault-access`/`:kit`/`:browse-ui`) + an instrumented Compose UI test + an on-device smoke asserting the real `.so` reveals the golden vault's `hunter2`. Pure Kotlin-port + Compose-UI slice — no `core` / `ffi` / `ios` / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-17-c3-android-reveal-on-tap-design.md`](docs/superpowers/specs/2026-06-17-c3-android-reveal-on-tap-design.md).
  - **C.3 slice 9 (Android) — soft-delete lifecycle** ✅ 2026-06-18 — the first Android slice that **writes** to a vault. A **Show deleted** toggle + per-row **Delete** (tombstone) / **Restore** (resurrect), mirroring iOS browse parity minus field editing (slice 10). Underneath sits the write infrastructure every later write reuses: a file-backed `FileDeviceUuidStore` (a non-secret 16-byte per-(install, vault) CRDT fingerprint via `SecureRandom`, persisted under `noBackupFilesDir` so a restored backup can't clone it, resolved once + cached per session) and the session write seam — `UniffiVaultSession.tombstoneRecord`/`resurrectRecord` project the **existing** uniffi write surface, serialized under the *same* `sessionLock` + `wiped` guard as reads (a write racing the lock-on-background `wipe()` can't touch zeroized handles). Toggling re-reads the block with `includeDeleted` (the Rust gate decides; the client never filters); a failed write surfaces a typed error and leaves the visible list intact (re-read on success only); new typed `RecordNotFound`/`SaveCryptoFailure` errors. Host-tested (`:vault-access` device-uuid + delete/restore/toggle, `:kit` error mapping), an instrumented Compose test (toggle + delete + restore), and an on-device round-trip smoke proving the real `.so` save-tail (delete → gone → show-deleted shows it tombstoned → restore → live), 4/4 (`:browse-ui`) + 6/6 (`:app`) on `Medium_Phone_API_36.1`. Pure Kotlin-port slice — no `core` / `ffi` / `ios` / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-17-c3-android-soft-delete-design.md`](docs/superpowers/specs/2026-06-17-c3-android-soft-delete-design.md).
  - **C.3 slice 10 (Android) — record add + edit** ✅ 2026-06-18 — full record **add** and **edit** on Android. A Compose `RecordEditForm` (free-text record type, editable tags, per-field Text/Bytes kind picker for text + bytes-as-hex fields, add/remove fields, per-row edit button in `BrowseScreen`) backed by `RecordEditModel` in `:browse-ui` — mirroring iOS `RecordEditViewModel` (add/edit/loadFailed modes, field-name/kind/value setters, add/remove field, add/set/remove tag). Drives `:kit`'s `VaultSession.appendRecord`/`editRecord` (new `VaultSession` write seam), which build a `RecordContent` via `toFfi` and call the existing uniffi `append_record`/`edit_record` surface under the same `sessionLock` + `wiped` guard as slice-9. Host-tested (`:vault-access` input types + `parseHex`, `:browse-ui` model, `:kit` real writers); instrumented Compose UI tests for the add and edit flows; on-device round-trip smoke: `append` then `edit` via the real `.so`, assert committed content survives close/reopen. 2/2 (`RecordEditFormTest`) + 6/6 (`:app`) + 4/4 (`:browse-ui`) on `Medium_Phone_API_36.1`. Pure Kotlin-port + Compose-UI slice — no `core` / `ffi` / `ios` / on-disk-format change.
  - **C.3 write-action debounce (Android + iOS)** ✅ 2026-06-18 (#254) — an in-flight guard so no write action (record Add/Edit commit, list Delete/Restore) executes twice from concurrent or rapid-repeat taps. The host-tested model owns the guard; the UI disables the button. **Android** `RecordEditModel.commit()` gains `inFlight` (concurrent-coroutine block) + `committed` (post-success render-gap re-tap block); `VaultBrowseModel` gains a global `writing` flag for delete/restore. Keystone test: two concurrent `commit()`s through an injected write gate → exactly one `appendRecord`; Compose disables Save/Delete/Restore/Add in flight (instrumented, `:browse-ui` 8/8 + `:app` 6/6 on `Medium_Phone_API_36.1`). **iOS** mirrors it: synchronous `commit()` guards on `committed` (its actual Add render-gap double-write fix) + `isWriting`; `VaultBrowseViewModel` gains `isWriting`; SwiftUI `.disabled` on the matching buttons (`swift test` 172/172; the `SecretaryApp` views compiled clean on the simulator via `run-ios-tests.sh` → `build-app.sh`). First cross-platform Android+iOS slice in C.3 — touches `ios/`, no `core` / `ffi` / on-disk-format change.
  - **C.3 sync-on-browse (Android)** ✅ 2026-06-18 — the sync badge + sync-at-unlock re-integrated onto the browse screen, completing the reunification of the two originally-separate `:app` flows. A new app-level `BrowseWithSyncScreen` stacks the (untouched) `SyncScreen` above `BrowseScreen`; `AppRoot`'s Browse route carries a `BrowseSession` (browse VM + sync VM + monitor); sync-at-unlock runs in the background off the render path via a copy-then-zeroize `launchSyncAtUnlock` helper (the password is cloned into a `ByteArray`, the Argon2id suspension fires, the clone is zeroed on return whether success or error). Monitor lifecycle bound to the Browse composition (start on entry, stop on dispose; a failed `monitor.start()` is non-fatal — the badge degrades gracefully); browse session still `lock()`-wiped on dispose. **Known accepted cost:** both platforms run a separate Argon2id for the sync pass (the sync coordinator opens the vault with the password per call on both platforms); the Android-specific delta is that Android cannot reuse the open session even for the vault UUID (iOS's `makeVaultSync(session:)` reads it from the session; Android provisions it via `goldenVaultUuid`); sync-pass Argon2id mitigated by background execution; restructuring `:kit` to share the session is out of scope. Mirrors iOS's unified `VaultBrowseScreen` (which lives in the app target). Android-only; no `:vault-access` / `:browse-ui` / `:sync-ui` library edits; no `core` / `ffi` / on-disk-format / UDL change. Spec + plan in [`docs/superpowers/specs/2026-06-18-c3-android-sync-on-browse-design.md`](docs/superpowers/specs/2026-06-18-c3-android-sync-on-browse-design.md) + [`docs/superpowers/plans/2026-06-18-c3-android-sync-on-browse.md`](docs/superpowers/plans/2026-06-18-c3-android-sync-on-browse.md).
  - **C.3 recovery-phrase open (Android)** ✅ 2026-06-19 — a second unlock credential (24-word BIP-39 recovery phrase) alongside the password open; both reach the unified `BrowseWithSyncScreen`. Sealed `UnlockCredential` + pure `openWithCredential` dispatch; `:kit` `openWithRecovery` over the generated `openVaultWithRecovery`; new `WrongRecoveryOrCorrupt` (conflated §13) + `InvalidRecoveryPhrase` typed errors. Recovery sessions get a status-only sync badge (Android sync is password-keyed) and sync manually via the badge re-prompt, mirroring iOS. Host-tested + an on-device recovery-open smoke. Android-only; no `core` / `ffi` / on-disk-format / UDL change. Spec + plan in [`docs/superpowers/specs/2026-06-18-c3-android-recovery-open-design.md`](docs/superpowers/specs/2026-06-18-c3-android-recovery-open-design.md) + [`docs/superpowers/plans/2026-06-18-c3-android-recovery-open.md`](docs/superpowers/plans/2026-06-18-c3-android-recovery-open.md).
  - **C.3 device-secret open slice 1 (Android)** ✅ 2026-06-19 — pure `DeviceUnlockCoordinator` + ports + `:kit` FFI adapter, proven against the real `.so` with a fake in-memory enclave; real biometric Keystore enclave + UnlockScreen toggle = slice 2. Android-only; no `core` / `ffi` / on-disk-format / UDL change.
  - **C.3 real biometric device open slice 2 (Android)** ✅ 2026-06-19 — `KeystoreDeviceSecretEnclave` (AES-256-GCM Android Keystore key; PRODUCTION config = auth-required + StrongBox-best-effort + `invalidatedByBiometricEnrollment`; `release` gated by `BiometricPrompt` via `CryptoObject`) + `FileDeviceEnrollmentMetadataStore` (`:kit`, host-tested) + `BiometricPromptGate` + pure `mapBiometricError` (`:app`) + `MainActivity` migrated `ComponentActivity` → `FragmentActivity` + `UnlockScreen` "Remember this device with biometrics" checkbox and "Unlock with biometrics" button + `AppRoot` enroll-on-password-unlock-with-remember and biometric-open → `BrowseWithSyncScreen` wiring. Automated coverage green (host suites, instrumented Keystore round-trip + `UnlockScreen` UI tests on emulator, slice-1 device-secret connected smoke regression). **Real-biometric on-device proof verified on an NX809J (Android 16, 2026-06-19):** enrol → kill → "Unlock with biometrics" released the device secret behind a real `BiometricPrompt` and opened the vault to `BrowseWithSyncScreen`, plus a clean cancel — the Android analogue of iOS's #202 Face ID proof. Android-only; no `core` / `ffi` / on-disk-format / UDL change.
  - **C.3 device-management Settings UI (Android)** ✅ 2026-06-19 — a "Device settings" screen reachable from the Browse screen showing this device's biometric-enrollment status; lets the user enroll (with a vault-password re-prompt) or disenroll this device. Android-only; no `core` / `ffi` / on-disk-format / UDL change.
  - **Android block-CRUD UI affordance (create/rename block + move record)** ✅ 2026-06-20 — Compose dialogs over the browse screen for the three already-shipped block-CRUD FFI ops: `create_block` (new block), `rename_block` (per-block rename), and `move_record` (per-record move to another block via a picker). Host-tested pure model + on-device create→move→read-back→tombstone round-trip over the real uniffi session. Android-only; no `core` / `ffi` / on-disk-format / UDL change.
  - **iOS block-CRUD UI affordance (create/rename block + move record)** ✅ 2026-06-20 — SwiftUI affordances over the browse stack for the three already-shipped block-CRUD FFI ops: `create_block` ("New block" toolbar button + block-name alert), `rename_block` (per-block Rename swipe action + block-name alert), and `move_record` (per-record Move swipe action → move-target picker sheet). Host-tested VM logic (create/rename/move guards + lock) + a real-FFI create→move→read-back round-trip. iOS-only; no `core` / `ffi` / on-disk-format / UDL change.
  - **Desktop (Tauri) block-CRUD UI affordance (rename block + move record)** ✅ 2026-06-20 — Svelte affordances over the Tauri browse UI for the two remaining block-CRUD ops: `rename_block` (per-block rename dialog) and `move_record` (per-record move dialog with target-block picker) — completing the block-CRUD tier on all three platforms (`create_block` already shipped in D.1.4). Desktop-only; no `core` / `ffi` / on-disk-format / UDL change.
  - **Android app — biometric write re-auth** ✅ 2026-06-21 — every mutating vault write requires a biometric presence proof first (30 s grace window, gate active iff device-secret is enrolled). Pure Kotlin `WriteReauthGate` / `BiometricAuthorizer` / `GraceWindowReauthGate` / `NoopReauthGate` in `:vault-access`; real `CoordinatorBiometricAuthorizer` in `:vault-access` (wraps `KeystoreDeviceSecretEnclave` + `BiometricPromptGate`). `VaultBrowseModel` + `RecordEditModel` call `guardedWrite`/`commitThenReload` with a typed reason before every mutation; cancel is silent, other failure surfaces `VaultBrowseError.ReauthFailed` and leaves any open dialog intact. Gate seeded at vault open, reset on lock. Host-tested (policy + gate + VM); on-device fingerprint/face is manual checklist item. Android-only; no `core` / `ffi` / on-disk-format change. **Deferred:** configurable/persisted grace-window setting; presence proof for password-only sessions without device-secret enrollment. Spec + plan in `docs/superpowers/specs/2026-06-21-android-write-reauth-design.md` + `docs/superpowers/plans/2026-06-21-android-write-reauth.md`.
  - **Desktop write-gate coverage guard ([#280](https://github.com/hherb/secretary/issues/280))** ✅ 2026-06-21 — a centralized vitest guard so a mutating IPC write can't silently ship ungated (the #278 review's `importContact` class). Every Tauri command is classified exactly once in `desktop/src/lib/writeCommands.ts` (read / write-gated / write-exempt / session) and diffed against the Rust `generate_handler!` list — a newly-registered command that isn't classified fails the test, forcing a conscious gate-vs-exempt decision. A pure, fixture-tested per-function source scanner (`desktop/src/lib/writeGateScanner.ts`) then rejects any gated-write wrapper called without a preceding `authorizeWrite` in the same handler — catching a sibling-gated handler masking an ungated one (the exact bug the #278 review caught by hand). The guard self-checks it isn't vacuous (asserts the scanned source set is non-empty + includes a known gated call site). Test-only; no runtime change to the shipped #278 surface. Desktop-only; no `core` / `ffi` / on-disk-format change. Spec + plan in [`docs/superpowers/specs/2026-06-21-desktop-write-gate-coverage-design.md`](docs/superpowers/specs/2026-06-21-desktop-write-gate-coverage-design.md) + [`docs/superpowers/plans/2026-06-21-desktop-write-gate-coverage.md`](docs/superpowers/plans/2026-06-21-desktop-write-gate-coverage.md). **Scanner method-shorthand gap closed ([#286](https://github.com/hherb/secretary/issues/286))** ✅ 2026-06-22 — `findFunctionBodies` now recognizes object/class method shorthand (`(async )?(get|set )?name(...) {`) as a per-function body, excluding control-flow heads (`if`/`for`/`while`/`switch`/`catch`/`with`) so a write nested in such a block is still attributed to its gated parent (no false positive). Closes the forward-looking blind spot where switching a handler to method shorthand would let a sibling gate mask an ungated write. Fixture-only regression coverage (no current desktop handler uses shorthand). Test-only; desktop-only.
  - **FFI secret-residue root fix ([#261](https://github.com/hherb/secretary/issues/261))** ✅ 2026-06-19 — the one-shot `MnemonicOutput.take_phrase()` and `DeviceSecretOutput.take_secret()` UDL accessors now return `bytes?` (was `sequence<u8>?`), so uniffi hands back a zeroizable `ByteArray?` (Kotlin) / `Data?` (Swift) instead of a non-overwritable boxed `List<UByte>` that lingered until GC. The Kotlin enroll consumer and the iOS enroll/create consumers drop the boxed-list copy (the iOS consumers also zero the transient `Data`); the Rust bridge impl (`Option<Vec<u8>>`) is unchanged, so there is no conformance-KAT byte-format impact. Verified: Swift + Kotlin conformance harnesses 27/27 each, both smoke runners, `:kit` host suite, full iOS acceptance. First UDL/FFI-surface change in the C.3 device series.
  - **C.3 remaining** ⏳ — `NSMetadataQuery` for iCloud-download-specific detection; optional `WorkManager` background detection (deferred from slice 3). Write re-auth deferred follow-ups: configurable/persisted grace-window settings; presence proof for password-only sessions with no device-secret enrollment (all platforms).
- **C.4 — Cross-device convergence conformance** ✅ 2026-06-15. Two real device identities (shared vault identity, distinct `device_uuid`s) editing through a shared folder converge to identical logical state (decrypted records + vector clocks + tombstone flags), order-independent, over the real `sync_once → prepare_merge → commit_with_decisions` pipeline. Four scenarios covered in [`core/tests/convergence.rs`](core/tests/convergence.rs): auto-apply (one editor; the other adopts), concurrent disjoint-field auto-merge, LWW field collision (later value wins), tombstone-veto (both decisions: AcceptTombstone is order-independent; KeepLocal is a per-replica user override, converges within an ordering). Convergence asserted on decrypted logical state, not ciphertext bytes (random nonces make bytes differ by design). Additive Rust test-only — no `core/src`, FFI, on-disk-format, or crypto change. **Python clean-room convergence mirror ✅ 2026-06-15** — the same four scenarios replayed in `conformance.py` using only stdlib generic crypto, proving order-independence from the spec docs alone (`core/tests/data/convergence_kat.json`). Deferred rungs: 3+ device topologies; and the durability/partition/clock-skew failure modes named in the original C.4 sketch (power-cycle crash-recovery is already covered by C.1.1b's idempotent recovery — this slice proves logical convergence + order-independence, not adversarial partition or clock-skew).

Sub-project C is where shippable software starts to exist. The `secretary sync` CLI alone is enough for a technically inclined user with a NAS to run a real multi-device vault.

---

## Sub-project D — Platform UIs 🚧 (D.1.1–D.1.16 ✅; D.1.17+ planned)

**Desktop** (macOS / Linux / Windows) is one Tauri 2 codebase (Rust backend + Svelte/TypeScript frontend). See [ADR-0007](docs/adr/0007-d-row-tauri.md) for the pivot from the original "Python + NiceGUI" desktop plan (ADR-0001) — the short version is that Tauri closes a localhost-HTTP attack surface, keeps secrets in Rust's deterministic-zeroize address space, and collapses the desktop UI to one codebase.

**Mobile** (iOS / Android) is built as **native apps** — Swift/SwiftUI and Kotlin/Jetpack Compose — consuming `secretary-core` through the **uniffi** bindings ([ffi/secretary-ffi-uniffi](ffi/secretary-ffi-uniffi/)). See [ADR-0008](docs/adr/0008-native-mobile-via-uniffi.md): the mobile portion of ADR-0007 (Tauri 2 mobile) was superseded after a 2026-06 evaluation found Tauri-2-mobile's hardware-backed-key-storage and biometric-bound-key-release story inadequate for this threat model. Native apps reach Keychain / Secure Enclave / Android Keystore / StrongBox + Face ID / BiometricPrompt directly. So the **uniffi binding is the mobile UI path** (not merely a third-party-consumer path), and the Python (`secretary-ffi-py`) binding remains the automation / scripting path.

Phase plan:

- **D.1 — Tauri walking skeleton (macOS + Linux desktop)**: unlock-an-existing-vault flow + block-list scaffold + auto-lock with vault-stored timeout settings + lock button. First end-to-end Tauri client proving the architecture. Decomposed as **D.1.1 ✅ shipped 2026-05-29 (walking skeleton)** → **D.1.2 ✅ shipped 2026-05-29 (read-only browse)** → **D.1.3 ✅ shipped 2026-05-29 (vault create wizard)** → **D.1.4 ✅ shipped 2026-05-30 (vault edit — add/edit records)** → **D.1.5 ✅ shipped 2026-05-31 (delete/trash)** → **D.1.6 ✅ shipped 2026-05-31 (share a block + contacts subsystem)** → **D.1.7 ✅ shipped 2026-06-03 (contacts management — export-my-card + Contacts pane + warn-but-allow delete; revoke deferred to [#177](https://github.com/hherb/secretary/issues/177))** → **D.1.8 ✅ shipped 2026-06-04 (per-block recipients — a read-only "Shared with" banner listing who a block is shared with; owner as "You", deleted-contact orphans as "Unknown contact" to keep the "delete ≠ revoke" residual keyholder visible)** → **D.1.9 ✅ shipped 2026-06-04 (per-contact reverse map — expand a contact to list the blocks they receive, click one to open it; inverts the `shared_block_count` scan; no core/binding change)** → **D.1.10 ✅ shipped 2026-06-05 (revoke primitive — frozen-`core` `revoke_block_recipient` re-keys a block for the remaining recipients + drops the revoked uuid + re-signs; shared `rewrite_block_with_recipients` engine; fail-fast `CannotRevokeOwner`; bridge `revoke_block_from`; spec §6.5.1/§7.3 + clean-room revoke KAT; closes [#177](https://github.com/hherb/secretary/issues/177))** → **D.1.11 ✅ shipped 2026-06-05 (desktop revoke UI — a "Revoke" ✕ on the "Shared with" banner + the per-contact reverse map, behind a forward-secrecy-aware confirm dialog; `revoke_block_from` IPC over the D.1.10 primitive; strict typed-error surfacing; owner never offered a control)** → **D.1.12 ✅ shipped 2026-06-05 (desktop polish batch — vendored inline-SVG icon system replacing every color-emoji icon (eight `currentColor` Lucide components, no runtime dep) [#154](https://github.com/hherb/secretary/issues/154); `aria-controls`↔`aria-expanded` on both disclosure toggles [#180](https://github.com/hherb/secretary/issues/180); `Esc`-pops-a-browse-level via a pure `shouldPopOnEscape` guard [#164](https://github.com/hherb/secretary/issues/164); `lock_session` hoisted into `commands::shared` across all eight command modules [#170](https://github.com/hherb/secretary/issues/170); no core/ffi/bridge change)** → **D.1.13 ✅ shipped 2026-06-06 (sync bridge primitive — `sync_pass_pause_on_conflict` orchestration auto-applies safe arms, returns `ConflictsPending` on tombstone veto; bridge `sync_status` + `sync_vault`; five new `FfiVaultError` sync variants; `sync_pass` KAT; sync functions bridge-only at ship; uniffi/pyo3 projection shipped separately ([#187](https://github.com/hherb/secretary/issues/187) ✅ 2026-06-09, pairs with [#167](https://github.com/hherb/secretary/issues/167)); no core/on-disk-format/crypto change)** → **D.1.14 ✅ shipped 2026-06-07 (desktop sync UI — TopBar "sync pill" showing "Synced …"/"Never synced" + "Sync now" trigger + password re-prompt modal; `sync_status`/`sync_now` Tauri commands; desktop DTOs; five sync `AppError` codes in TS; no core/ffi/FfiVaultError/UDL/bridge change)** → **D.1.15 ✅ shipped 2026-06-08 (interactive conflict resolution — turns the `ConflictsPending` veto into a per-record **Keep mine / Accept delete** modal listing each disputed record by metadata only (type · tags · field names · timestamps · deleting device; no secret values) + a read-only "auto-merged" LWW notice, default Keep mine so no silent data loss; a stateless recompute-on-commit two-call flow — `sync_pass_inspect` returns the veto + a `manifest_hash` freshness token committing nothing, `sync_pass_commit_decisions` recomputes the deterministic draft and rejects with `EvidenceStale` on a mid-modal disk change before committing; spans `core` (a metadata-only `RecordCollisionSummary`) + cli + bridge (`VetoDto`/`CollisionDto`/`VetoDecisionDto` + `sync_commit_decisions` + a `SyncDecisionsIncomplete` error un-collapsed from `SyncFailed`) + uniffi/pyo3 (new `FfiVaultError` variant threaded through all bindings + the Swift/Kotlin conformance harnesses) + desktop (`ConflictResolutionDialog` wired into the SyncPill flow, reusing the sync password); commit happy-path + stale-token rejection covered at the cli layer, manual GUI smoke still deferred ([#161](https://github.com/hherb/secretary/issues/161))))** → **D.1.16 ✅ shipped 2026-06-20 (desktop block-CRUD UI affordance — `rename_block` (per-block rename dialog) and `move_record` (per-record move dialog with target-block picker) wired over the existing Tauri IPC commands, completing the block-CRUD tier on all three platforms; `create_block` already shipped in D.1.4; desktop-only, no `core` / `ffi` / on-disk-format change)**. D.1.1 spec + plan + ADR 0007 in [`docs/superpowers/plans/2026-05-27-d11-tauri-walking-skeleton.md`](docs/superpowers/plans/2026-05-27-d11-tauri-walking-skeleton.md); D.1.2 spec + plan in [`docs/superpowers/plans/2026-05-29-d12-browse.md`](docs/superpowers/plans/2026-05-29-d12-browse.md); D.1.4 spec + plan in [`docs/superpowers/specs/2026-05-30-d14-vault-edit-design.md`](docs/superpowers/specs/2026-05-30-d14-vault-edit-design.md); D.1.15 spec + plan in [`docs/superpowers/specs/2026-06-07-interactive-conflict-resolution-design.md`](docs/superpowers/specs/2026-06-07-interactive-conflict-resolution-design.md) + [`docs/superpowers/plans/2026-06-07-interactive-conflict-resolution.md`](docs/superpowers/plans/2026-06-07-interactive-conflict-resolution.md); the L4 end-to-end test is manual-only and deferred ([#161](https://github.com/hherb/secretary/issues/161)).
- **D.2 — Linux + Windows desktop maturation**: CI matrix, distribution packaging (`.deb` / `.AppImage` / `.rpm` / `.msi`), code signing, notarization. Windows is not a primary target but the Tauri codebase supports it; contributors are welcome.
- **D.3 — Native mobile (iOS + Android)** ([ADR-0008](docs/adr/0008-native-mobile-via-uniffi.md)): Swift/SwiftUI (iOS) and Kotlin/Jetpack Compose (Android) apps consuming `secretary-core` via the uniffi bindings (XCFramework / `.aar`), with first-party Keychain / Secure Enclave / Android Keystore / StrongBox + Face ID / BiometricPrompt for hardware-backed, biometric-bound key release; plus App Store / Play Store packaging and Android AutoFill Service.
- **D.4 — Browser autofill extensions** ([ADR-0010](docs/adr/0010-browser-autofill-native-messaging.md)): a thin-courier WebExtension talking to a local native-messaging host (no listening socket; per-fill vault open via the existing `open_with_device_secret` verify-before-decrypt path). Decomposed as **D.4.1 ✅ shipped 2026-06-16 (native-messaging walking skeleton — a Rust host crate `browser/secretary-browser-host` with a 1 MiB-capped never-panic framing codec + `query → available{count:0}` no-op loop, a Chromium MV3 extension, and a dev host manifest; pure transport, no `secretary-core` dep, no secrets; design + 6-task plan in [`docs/superpowers/specs/2026-06-16-d41-native-messaging-skeleton-plan.md`](docs/superpowers/specs/2026-06-16-d41-native-messaging-skeleton-plan.md))** → **D.4.2 ✅ shipped 2026-06-16 (per-fill open — the host adds the `secretary-core` dep and opens the casual vault per `query` via the existing `open_vault(Unlocker::DeviceSecret)` verify-before-decrypt path, replying `available{count}` with the live block count; a `DeviceSecretSource` port + dev-only file provider (real OS keystore deferred), helper-local config, and a dev `secretary-browser-enroll` tool over the genuine `add_device_slot`; no key material between fills, no secrets cross, casual vault only; plan in [`docs/superpowers/specs/2026-06-16-d42-per-fill-open-plan.md`](docs/superpowers/specs/2026-06-16-d42-per-fill-open-plan.md))** → **D.4.3 ⏳ in progress (origin-matching engine — the security-critical slice; the pure engine + pinned `origin_match_kat.json` (24 vectors) are shipped: HTTPS-only, top-frame-governs, `exact_origin` + `registrable_domain` via an exact-pinned PSL, IDN-homograph-safe; the origin-aware `per_fill_count` integration is pending a record-read decision — see [`docs/handoffs/2026-06-16-d43-engine-core-shipped.md`](docs/handoffs/2026-06-16-d43-engine-core-shipped.md); plan in [`docs/superpowers/specs/2026-06-16-d43-origin-matching-plan.md`](docs/superpowers/specs/2026-06-16-d43-origin-matching-plan.md)) — then the real OS-keystore secret source + desktop enrollment UI** → **D.4.4 (click-to-fill + native confirmation + injection)** → **D.4.5 (`origin_binding` metadata + guard-rails)** → **D.4.6 (Firefox/Safari/Windows + signing)** → **D.4.7 (write/capture)**. D.4 design in [`docs/superpowers/specs/2026-06-15-d4-browser-autofill-design.md`](docs/superpowers/specs/2026-06-15-d4-browser-autofill-design.md); the browser track runs parallel to the main line with its handoff in [`NEXTSESSION_BROWSER_PLUGIN.md`](NEXTSESSION_BROWSER_PLUGIN.md).

Each phase is independent — they can ship in any order and at independent paces. D.1 is first because the desktop walking skeleton proves the core/UI architecture before the native-mobile (D.3) work, which carries the platform-security-framework integration.

---

## Out of scope for v1

These are explicit non-goals for the first release. Some may move into v2; some are permanently excluded.

- **Server-mediated sync.** No backend. Sync is via cloud folders the user already has (iCloud, Drive, Dropbox, OneDrive, WebDAV, USB stick). See [ADR-0003](docs/adr/0003-cloud-folder-sync.md).
- **Real-time push notifications.** Sync happens on next file-system event from the cloud-folder client.
- **Anonymity / metadata privacy from the cloud-folder host.** The host can see encrypted file sizes, modification times, and recipient counts (the recipient table is part of the file). Plaintext is never visible.
- **Coercion resistance / plausible deniability.** Considered for a future format version.
- **Server-side policy enforcement.** No server, so no enforcement.
- **Defence against compromise of the OS, hardware, or trusted computing base.** Standard assumption for client-side crypto.

---

## How contributors can help

The project is currently a solo effort and intentionally gated on cryptographic correctness — most work happens behind specifications and audited PRs rather than through ad-hoc contributions. That said, helpful avenues right now:

- **Review the design docs.** [docs/threat-model.md](docs/threat-model.md), [docs/crypto-design.md](docs/crypto-design.md), [docs/vault-format.md](docs/vault-format.md), and [docs/adr/](docs/adr/) are the source of truth. Ambiguities or errors there have outsized impact. Open issues against the docs.
- **Build a clean-room implementation.** Use only `docs/` and verify against [core/tests/python/conformance.py](core/tests/python/conformance.py) (full hybrid-decap + AEAD-decrypt + hybrid-verify against `golden_vault_001/`, using generic crypto primitives via PEP 723 with no dependency on `secretary-core`). If your implementation works without reading the Rust source, the spec is doing its job. If it doesn't, please open an issue.
- **Cryptographic review.** Independent scrutiny of the hybrid constructions, KAT coverage, and AAD/signed-range definitions is welcome. Especially valuable: someone with FIPS 203 / FIPS 204 implementer experience.
- **Wait for Sub-project D.** If your interest is the platform UI layer, that phase hasn't started. Star the repo and check back.

PRs against the Rust core are accepted but the bar is high: every change must come with KAT-level tests, no `unsafe`, typed errors only, and adherence to the conventions documented in `secretary_next_session.md` and the existing source.

---

## Cadence

This is intentionally a slow project. A vault format that protects multi-decade secrets is not the right place to optimize for time-to-MVP. Releases happen when the work is right, not when a calendar says so.

Rough order-of-magnitude expectations (no commitments):

- **Sub-project A complete + audited**: months, not weeks.
- **Sub-project B (FFI)**: weeks once A is frozen.
- **Sub-project C (sync orchestration + headless CLI)**: weeks-to-months on top of B; the C.1 state machine and C.2 CLI are bounded, the C.3 mobile adapters depend on per-platform OS work.
- **Sub-project D.1 (Tauri walking skeleton + feature slices on macOS / Linux desktop)**: weeks-to-months on top of C — Tauri scaffold is one-time cost; per-feature slices (D.1.1 → D.1.5) are bounded.
- **Sub-project D.2 (Linux + Windows desktop maturation)**: weeks after D.1; CI matrix + packaging + signing.
- **Sub-project D.3 (native mobile — iOS + Android via uniffi, [ADR-0008](docs/adr/0008-native-mobile-via-uniffi.md))**: parallelisable with D.2; native SwiftUI / Compose over the existing uniffi bindings, carrying the platform-security-framework (Keychain / Secure Enclave / Keystore / biometric) integration. Pace depends on contributors.
- **v1.0 release**: when all of the above ship and the spec has been stable across at least one external review cycle.

The project will not have a "1.0" tag until the cryptographic foundation has stood up to independent scrutiny and the first reference UI is real software people can use to store real secrets.
